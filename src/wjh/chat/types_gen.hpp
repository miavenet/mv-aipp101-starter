#ifndef WJH_CHAT_99907B3D5E158A904F8A5A49493807EF0C5F5473
#define WJH_CHAT_99907B3D5E158A904F8A5A49493807EF0C5F5473

// ======================================================================
// NOTICE  NOTICE  NOTICE  NOTICE  NOTICE  NOTICE  NOTICE  NOTICE  NOTICE
// ----------------------------------------------------------------------
//
// DO NOT EDIT THIS FILE DIRECTLY.
//
// This source file has been generated by Atlas Strong Type Generator v2.0.0
// https://github.com/jodyhagins/Atlas
//
// DO NOT EDIT THIS FILE DIRECTLY.
//
// ----------------------------------------------------------------------
// NOTICE  NOTICE  NOTICE  NOTICE  NOTICE  NOTICE  NOTICE  NOTICE  NOTICE
// ======================================================================

#if __has_include(<version>)
#include <version>
#endif
#include <concepts>
#include <cstdint>
#include <format>
#include <functional>
#include <iostream>
#include <sstream>
#include <stdexcept>
#include <string>
#include <type_traits>
#include <utility>

#ifndef WJH_ATLAS_50E620B544874CB8BE4412EE6773BF90
#define WJH_ATLAS_50E620B544874CB8BE4412EE6773BF90

// Version of the shared boilerplate compiled under the guard above.  Every
// generated header verifies it after this section, so mixing headers from
// incompatible Atlas versions in one translation unit fails with one clear
// message instead of confusing errors inside generated classes.
#define WJH_ATLAS_PREAMBLE_VERSION 2

// ======================================================================
// ATLAS STRONG TYPE BOILERPLATE
// ----------------------------------------------------------------------
//
// This section provides the infrastructure for Atlas strong types.
// It is identical across all Atlas-generated files and uses a shared
// header guard (WJH_ATLAS_50E620B544874CB8BE4412EE6773BF90) to ensure
// the boilerplate is only included once even when multiple generated
// files are used in the same translation unit.
//
// The boilerplate is intentionally inlined to make generated code
// self-contained with zero external dependencies.
//
// Components:
// - atlas::strong_type_tag: Base class for strong types
// - atlas::undress(): Universal value accessor for strong types
// - atlas_detail::*: Internal implementation utilities
//
// For projects using multiple Atlas-generated files, this boilerplate
// will only be compiled once per translation unit thanks to the shared
// header guard below.
//
// ----------------------------------------------------------------------
// DO NOT EDIT THIS SECTION
// ======================================================================

// Atlas feature detection macros
#ifndef ATLAS_NODISCARD
#if defined(__cpp_attributes) && __cpp_attributes >= 201603L
#define ATLAS_NODISCARD [[nodiscard]]
#else
#define ATLAS_NODISCARD
#endif
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
#include <compare>
#endif

#if defined(__cpp_lib_format) && __cpp_lib_format >= 202110L
#include <format>
#endif

// The borrow-classification seeds below name std::basic_string_view and
// std::span, so their headers are included unconditionally at the standards
// that provide them.  No preamble text may depend on a per-type property:
// this section must stay byte-identical across every generated header.
#if defined(__cpp_lib_string_view) && __cpp_lib_string_view >= 201606L
#include <string_view>
#endif

#if defined(__cpp_lib_span) && __cpp_lib_span >= 202002L
#include <span>
#endif

#if defined(__cpp_lib_ranges) && __cpp_lib_ranges >= 201911L
#include <ranges>
#endif

namespace atlas {

template<typename T>
struct strong_type_tag
{
#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    friend auto operator <=> (
        strong_type_tag const &,
        strong_type_tag const &) = default;
#endif
};

struct value_tag
{ };

namespace atlas_detail {

template <typename... Ts>
struct make_void
{
    using type = void;
};

template <typename... Ts>
using void_t = typename make_void<Ts...>::type;

template <std::size_t N>
struct PriorityTag
: PriorityTag<N - 1>
{ };

template <>
struct PriorityTag<0u>
{ };

using value_tag = PriorityTag<3>;

template <bool B>
using bool_c = std::integral_constant<bool, B>;
template <typename T>
using bool_ = bool_c<T::value>;
template <typename T>
using not_ = bool_c<not T::value>;
template <typename T, typename U>
using and_ = bool_c<T::value && U::value>;
template <typename T>
using is_lref = std::is_lvalue_reference<T>;

template <typename T>
using remove_cv_t = typename std::remove_cv<T>::type;
template <typename T>
using remove_reference_t = typename std::remove_reference<T>::type;
template <typename T>
using remove_cvref_t = remove_cv_t<remove_reference_t<T>>;
template <bool B, typename T = void>
using enable_if_t = typename std::enable_if<B, T>::type;
template <bool B>
using when = enable_if_t<B, bool>;

template <typename T>
using _t = typename T::type;

template <typename T, typename = void>
struct has_atlas_value_type
: std::false_type
{ };

template <typename T>
struct has_atlas_value_type<
    T,
    enable_if_t<not std::is_same<
        typename remove_cvref_t<T>::atlas_value_type,
        void>::value>>
: std::true_type
{ };

// ----------------------------------------------------------------------------
// unwrap_type: remove exactly one layer, at the type level.
//
// Mirrors atlas::unwrap, including its SFINAE behavior: there is no nested
// ::type when T is neither an atlas type nor an enum, so unwrap_t<T> is a
// substitution failure rather than a hard error.
// ----------------------------------------------------------------------------
template <typename T, typename = void>
struct unwrap_type
{ };

template <typename T>
struct unwrap_type<T, enable_if_t<has_atlas_value_type<T>::value>>
{
    using type = typename remove_cvref_t<T>::atlas_value_type;
};

template <typename T>
struct unwrap_type<
    T,
    enable_if_t<
        not has_atlas_value_type<T>::value &&
        std::is_enum<remove_cvref_t<T>>::value>>
{
    using type = typename std::underlying_type<remove_cvref_t<T>>::type;
};

// ----------------------------------------------------------------------------
// undress_type: drill through every layer, at the type level.
//
// Mirrors atlas::undress: recurses through atlas layers, drills a resulting
// enum to its underlying type, and is the identity for everything else, so
// undress_t<T> is always well-formed.
// ----------------------------------------------------------------------------
template <typename T, typename = void>
struct undress_type
{
    using type = remove_cvref_t<T>;
};

template <typename T>
struct undress_type<T, enable_if_t<has_atlas_value_type<T>::value>>
: undress_type<typename remove_cvref_t<T>::atlas_value_type>
{ };

template <typename T>
struct undress_type<
    T,
    enable_if_t<
        not has_atlas_value_type<T>::value &&
        std::is_enum<remove_cvref_t<T>>::value>>
{
    using type = typename std::underlying_type<remove_cvref_t<T>>::type;
};

} // namespace atlas_detail

// ----------------------------------------------------------------------------
// Borrow classification
//
// A conversion is dangerous when the target borrows storage that the source
// owns: converting a temporary then dangles.  Neither type alone decides it,
// so classification is a two-type predicate, built from three pieces:
//
// - borrowed_type<T>: C++11-compatible opt-in.  Specialize to std::true_type
//   for any type that refers to storage it does not own.  Seeded for the
//   standard view types; raw pointers are deliberately not seeded (classifying
//   void* would false-positive on every opaque handle).
// - is_borrowed<T>: the query.  Also honors
//   std::ranges::enable_borrowed_range where the library provides it, as a
//   disjunction of expressions -- not competing partial specializations -- so
//   double opt-in is not ambiguous.  An atlas type is classified by what it
//   wraps: a wrapper is borrowed when any layer beneath it is, so a
//   wrapper-of-view chain is a borrowing type at every depth.
// - borrow_policy<TargetT, UnderlyingT>: THE predicate every generated guard
//   queries.  True when the target borrows and the underlying owns.
//   Specializable per pair in either direction: relax it for a backing that
//   keeps storage alive independently, or tighten it for a pair neither
//   mechanism classified.
// ----------------------------------------------------------------------------
template <typename T>
struct borrowed_type
: std::false_type
{ };

#if defined(__cpp_lib_string_view) && __cpp_lib_string_view >= 201606L
template <typename CharT, typename TraitsT>
struct borrowed_type<std::basic_string_view<CharT, TraitsT>>
: std::true_type
{ };
#endif

#if defined(__cpp_lib_span) && __cpp_lib_span >= 202002L
template <typename T, std::size_t ExtentV>
struct borrowed_type<std::span<T, ExtentV>>
: std::true_type
{ };
#endif

template <typename T>
struct is_borrowed;

namespace atlas_detail {

// An atlas-generated type borrows when the value it wraps borrows:
// classification recurses layer by layer, so any borrowed layer -- root or
// intermediate -- makes every wrapper above it a borrowing type.  The
// recursion is limited to types deriving from strong_type_tag: a generated
// wrapper's atlas_value_for is a pure pass-through of the member its
// atlas_value_type names, so the wrapped type's classification transfers.
// A hand-written type that adopts the atlas_value_type protocol may OWN
// storage and compute a view from it in atlas_value_for -- classifying it
// as borrowed would disarm the guards and let that view dangle -- so such
// types keep the explicit opt-in contract (atlas::borrowed_type or
// std::ranges::enable_borrowed_range).
template <typename T,
    bool = has_atlas_value_type<T>::value &&
        std::is_base_of<
            strong_type_tag<remove_cvref_t<T>>,
            remove_cvref_t<T>>::value>
struct wraps_borrowed
: std::false_type
{ };

template <typename T>
struct wraps_borrowed<T, true>
: is_borrowed<typename remove_cvref_t<T>::atlas_value_type>
{ };

} // namespace atlas_detail

template <typename T>
struct is_borrowed
: std::integral_constant<bool,
    borrowed_type<atlas_detail::remove_cvref_t<T>>::value
#if defined(__cpp_lib_ranges) && __cpp_lib_ranges >= 201911L
    || std::ranges::enable_borrowed_range<atlas_detail::remove_cvref_t<T>>
#endif
    || atlas_detail::wraps_borrowed<atlas_detail::remove_cvref_t<T>>::value>
{ };

template <typename TargetT, typename UnderlyingT, typename = void>
struct borrow_policy
: std::integral_constant<bool,
    is_borrowed<TargetT>::value && not is_borrowed<UnderlyingT>::value>
{ };

namespace atlas_detail {

void atlas_value_for();
struct value_by_ref
{ };
struct value_by_val
{ };
struct ref_tag
{ };

// ----------------------------------------------------------------------------
// Base case: T does not have atlas_value_type
// These are the termination cases for the recursion.
// ----------------------------------------------------------------------------
template <typename T>
constexpr T &
value_impl(T & t, PriorityTag<0>, value_by_ref)
{
    return t;
}
template <typename T>
constexpr T const &
value_impl(T const & t, PriorityTag<0>, value_by_ref)
{
    return t;
}
template <typename T>
constexpr T
value_impl(T & t, PriorityTag<0>, value_by_val)
{
    return std::move(t);
}
template <typename T>
constexpr T
value_impl(T const & t, PriorityTag<0>, value_by_val)
{
    return t;
}

// ----------------------------------------------------------------------------
// Enum case: T is an enum - convert to underlying type
// Always returns by value since conversion creates a distinct value.
// ----------------------------------------------------------------------------
template <typename T>
constexpr auto
value_impl(T t, PriorityTag<2>, value_by_ref)
-> typename std::enable_if<
    std::is_enum<T>::value,
    typename std::underlying_type<T>::type>::type
{
    return static_cast<typename std::underlying_type<T>::type>(t);
}
template <typename T>
constexpr auto
value_impl(T t, PriorityTag<2>, value_by_val)
-> typename std::enable_if<
    std::is_enum<T>::value,
    typename std::underlying_type<T>::type>::type
{
    return static_cast<typename std::underlying_type<T>::type>(t);
}

// ----------------------------------------------------------------------------
// Recursive case: T has atlas_value_for() hidden friend
// Use ADL to call atlas_value_for() and recurse.
// ----------------------------------------------------------------------------
template <typename T>
constexpr auto
value_impl(T & t, PriorityTag<1>, value_by_ref)
-> decltype(value_impl(
    atlas_value_for(t),
    value_tag{},
    value_by_ref{}))
{
    return value_impl(atlas_value_for(t), value_tag{}, value_by_ref{});
}
template <typename T>
constexpr auto
value_impl(T const & t, PriorityTag<1>, value_by_ref)
-> decltype(value_impl(
    atlas_value_for(t),
    value_tag{},
    value_by_ref{}))
{
    return value_impl(atlas_value_for(t), value_tag{}, value_by_ref{});
}
template <typename T>
constexpr auto
value_impl(T & t, PriorityTag<1>, value_by_val)
-> decltype(value_impl(
    atlas_value_for(std::move(t)),
    value_tag{},
    value_by_val{}))
{
    return value_impl(atlas_value_for(std::move(t)), value_tag{}, value_by_val{});
}
template <typename T>
constexpr auto
value_impl(T const & t, PriorityTag<1>, value_by_val)
-> decltype(value_impl(
    atlas_value_for(t),
    value_tag{},
    value_by_val{}))
{
    return value_impl(atlas_value_for(t), value_tag{}, value_by_val{});
}

struct ToUnderlying
{
    template <typename T>
    constexpr auto
    operator () (T & t) const
    -> decltype(atlas_detail::value_impl(t, value_tag{}, value_by_ref{}))
    {
        return atlas_detail::value_impl(t, value_tag{}, value_by_ref{});
    }

    template <typename T>
    constexpr auto
    operator () (T const & t) const
    -> decltype(atlas_detail::value_impl(t, value_tag{}, value_by_ref{}))
    {
        return atlas_detail::value_impl(t, value_tag{}, value_by_ref{});
    }

    template <
        typename T,
        when<not std::is_lvalue_reference<T>::value> = true>
    constexpr auto
    operator () (T && t) const
    -> decltype(atlas_detail::value_impl(t, value_tag{}, value_by_val{}))
    {
        return atlas_detail::value_impl(t, value_tag{}, value_by_val{});
    }
};

// ----------------------------------------------------------------------------
// Unwrap: Remove exactly one layer from atlas types or enums
// Unlike undress, this does NOT recurse.
// ----------------------------------------------------------------------------

template <typename T>
constexpr auto
unwrap_impl(T & t, PriorityTag<2>)
-> decltype(atlas_value_for(t))
{
    return atlas_value_for(t);
}

template <typename T>
constexpr auto
unwrap_impl(T const & t, PriorityTag<2>)
-> decltype(atlas_value_for(t))
{
    return atlas_value_for(t);
}

template <typename T>
constexpr auto
unwrap_impl(T && t, PriorityTag<2>)
-> typename std::enable_if<
    not std::is_lvalue_reference<T>::value,
    decltype(atlas_value_for(std::move(t)))>::type
{
    return atlas_value_for(std::move(t));
}

// Enum fallback - convert to underlying type
template <typename T>
constexpr auto
unwrap_impl(T t, PriorityTag<1>)
-> typename std::enable_if<
    std::is_enum<T>::value,
    typename std::underlying_type<T>::type>::type
{
    return static_cast<typename std::underlying_type<T>::type>(t);
}

// No PriorityTag<0> - SFINAE failure for non-atlas/non-enum types

struct Unwrap
{
    template <typename T>
    constexpr auto
    operator () (T & t) const
    -> decltype(unwrap_impl(t, PriorityTag<2>{}))
    {
        return unwrap_impl(t, PriorityTag<2>{});
    }

    template <typename T>
    constexpr auto
    operator () (T const & t) const
    -> decltype(unwrap_impl(t, PriorityTag<2>{}))
    {
        return unwrap_impl(t, PriorityTag<2>{});
    }

    template <
        typename T,
        when<not std::is_lvalue_reference<T>::value> = true>
    constexpr auto
    operator () (T && t) const
    -> decltype(unwrap_impl(std::forward<T>(t), PriorityTag<2>{}))
    {
        return unwrap_impl(std::forward<T>(t), PriorityTag<2>{});
    }
};

// ----------------------------------------------------------------------------
// UndressEnum: Drill through atlas types and stop at enum
// Like undress, but stops at enum instead of converting to underlying type.
// SFINAE fails if the drill does not resolve to an enum.
// ----------------------------------------------------------------------------
using undress_enum_tag = PriorityTag<1>;

// Base case: T is an enum - return it (don't convert to underlying)
template <typename T>
constexpr auto
undress_enum_impl(T & t, PriorityTag<0>)
-> typename std::enable_if<std::is_enum<T>::value, T &>::type
{
    return t;
}

template <typename T>
constexpr auto
undress_enum_impl(T const & t, PriorityTag<0>)
-> typename std::enable_if<std::is_enum<T>::value, T const &>::type
{
    return t;
}

// Base case: rvalue enum - return by value (avoids dangling reference)
template <typename T>
constexpr auto
undress_enum_impl(T && t, PriorityTag<0>)
-> typename std::enable_if<
    not std::is_lvalue_reference<T>::value && std::is_enum<T>::value,
    T>::type
{
    return t;
}

// Recursive case: drill through atlas types
template <typename T>
constexpr auto
undress_enum_impl(T & t, PriorityTag<1>)
-> decltype(undress_enum_impl(atlas_value_for(t), undress_enum_tag{}))
{
    return undress_enum_impl(atlas_value_for(t), undress_enum_tag{});
}

template <typename T>
constexpr auto
undress_enum_impl(T const & t, PriorityTag<1>)
-> decltype(undress_enum_impl(atlas_value_for(t), undress_enum_tag{}))
{
    return undress_enum_impl(atlas_value_for(t), undress_enum_tag{});
}

template <typename T>
constexpr auto
undress_enum_impl(T && t, PriorityTag<1>)
-> typename std::enable_if<
    not std::is_lvalue_reference<T>::value,
    decltype(undress_enum_impl(atlas_value_for(std::move(t)), undress_enum_tag{}))>::type
{
    return undress_enum_impl(atlas_value_for(std::move(t)), undress_enum_tag{});
}

struct UndressEnum
{
    template <typename T>
    constexpr auto
    operator () (T & t) const
    -> decltype(undress_enum_impl(t, undress_enum_tag{}))
    {
        return undress_enum_impl(t, undress_enum_tag{});
    }

    template <typename T>
    constexpr auto
    operator () (T const & t) const
    -> decltype(undress_enum_impl(t, undress_enum_tag{}))
    {
        return undress_enum_impl(t, undress_enum_tag{});
    }

    template <
        typename T,
        when<not std::is_lvalue_reference<T>::value> = true>
    constexpr auto
    operator () (T && t) const
    -> decltype(undress_enum_impl(std::forward<T>(t), undress_enum_tag{}))
    {
        return undress_enum_impl(std::forward<T>(t), undress_enum_tag{});
    }
};

// ----------------------------------------------------------------------------
// Type trait: holds_enum - true if undress_enum would succeed
// Uses the same mechanism as undress_enum to ensure consistency.
// ----------------------------------------------------------------------------
template <typename T, typename = void>
struct holds_enum_impl : std::false_type {};

template <typename T>
struct holds_enum_impl<
    T,
    void_t<decltype(undress_enum_impl(
        std::declval<remove_cvref_t<T> &>(),
        undress_enum_tag{}))>>
: std::true_type {};

using cast_tag = PriorityTag<1>;

// For reference targets, a direct cast must bind to the source object
// itself -- the target type is an actual layer -- and never materialize a
// temporary through a conversion function or constructor: a reference into
// such a temporary dangles the moment the cast returns.  Value targets are
// unconstrained.
template <typename TargetT, typename U>
struct is_ref_bind_safe
: bool_c<
    not std::is_reference<TargetT>::value ||
    std::is_same<remove_cvref_t<TargetT>, remove_cvref_t<U>>::value ||
    std::is_base_of<remove_cvref_t<TargetT>, remove_cvref_t<U>>::value>
{};

// Trait to check if static_cast<TargetT>(U) is well-formed, and, for
// reference targets, binds directly rather than to a materialized temporary.
template <typename TargetT, typename U, typename = void>
struct is_directly_castable : std::false_type {};

template <typename TargetT, typename U>
struct is_directly_castable<TargetT, U,
    void_t<decltype(static_cast<TargetT>(std::declval<U>()))>>
: is_ref_bind_safe<TargetT, U> {};

// Recursive castability check via struct template specialization.
// GCC evaluates trailing return types of function template overloads eagerly
// during overload resolution -- before checking enable_if default arguments --
// which causes infinite recursion when the trailing return type contains a
// recursive call to cast_impl.  Struct template partial specializations do not
// have this problem: the enable_if in the third parameter is fully resolved
// before the base class (which carries the recursive check) is instantiated.
template <typename TargetT, typename U, typename = void>
struct can_cast_impl;

// True when unwrapping U to reach TargetT would borrow from an expiring
// owner: the source is not an lvalue and borrow_policy holds for the target
// against what one unwrap step yields.  Keyed on borrow_policy directly --
// NOT on probing for a guard overload on U -- so drilling is blocked even for
// types that declare no conversion clause at all.
template <typename TargetT, typename U, typename = void>
struct is_lifetime_guarded_cast : std::false_type {};

template <typename TargetT, typename U>
struct is_lifetime_guarded_cast<TargetT, U,
    void_t<decltype(atlas_value_for(std::declval<U>()))>>
: bool_c<
    not std::is_lvalue_reference<U>::value &&
    borrow_policy<
        TargetT,
        remove_cvref_t<decltype(atlas_value_for(std::declval<U>()))>>::value>
{};

template <typename TargetT, typename U, typename = void>
struct can_cast_via_unwrap : std::false_type {};

// std::conditional (not and_) so the recursive can_cast_impl base is only
// instantiated when the lifetime guard does not already reject the step.
template <typename TargetT, typename U>
struct can_cast_via_unwrap<TargetT, U,
    void_t<decltype(atlas_value_for(std::declval<U>()))>>
: std::conditional<
    is_lifetime_guarded_cast<TargetT, U>::value,
    std::false_type,
    can_cast_impl<TargetT, decltype(atlas_value_for(std::declval<U>()))>
  >::type {};

template <typename TargetT, typename U, typename>
struct can_cast_impl : can_cast_via_unwrap<TargetT, U> {};

// A valid static_cast terminates the search for value targets and for
// directly-bound reference targets; a merely materializing reference cast
// keeps drilling in case an actual layer of the target type lies deeper.
template <typename TargetT, typename U>
struct can_cast_impl<TargetT, U,
    void_t<decltype(static_cast<TargetT>(std::declval<U>()))>>
: std::conditional<
    is_ref_bind_safe<TargetT, U>::value,
    std::true_type,
    can_cast_via_unwrap<TargetT, U>
  >::type {};

// ----------------------------------------------------------------------------
// cast_impl: Drill down to find the first type castable to TargetT
//
// Reference targets reject rvalue sources outright: cast<T const &> or
// cast<T &&> from a temporary would return a reference into a value that
// dies at the end of the full expression.  Bind the source to a named
// variable first.
// ----------------------------------------------------------------------------
template <
    typename TargetT, typename U,
    typename std::enable_if<
        is_ref_bind_safe<TargetT, U>::value &&
        (!std::is_reference<TargetT>::value ||
         std::is_lvalue_reference<U>::value),
        bool>::type = true>
constexpr auto
cast_impl(U && u, PriorityTag<1>)
-> decltype(static_cast<TargetT>(std::forward<U>(u)))
{
    return static_cast<TargetT>(std::forward<U>(u));
}

// Drill one layer.  Lvalue sources unwrap to references; rvalue sources
// unwrap by value (each layer is moved out), so value targets never hold a
// reference into an expired layer.
template <
    typename TargetT, typename U,
    typename std::enable_if<
        can_cast_impl<TargetT, U>::value &&
        !is_directly_castable<TargetT, U>::value &&
        (!std::is_reference<TargetT>::value ||
         std::is_lvalue_reference<U>::value),
        bool>::type = true>
constexpr TargetT
cast_impl(U && u, PriorityTag<0>)
{
    return cast_impl<TargetT>(atlas_value_for(std::forward<U>(u)), cast_tag{});
}

template <typename TargetT>
struct CastTo
{
    template <typename U>
    constexpr auto
    operator () (U && u) const
    -> decltype(cast_impl<TargetT>(std::forward<U>(u), cast_tag{}))
    {
        return cast_impl<TargetT>(std::forward<U>(u), cast_tag{});
    }
};

void begin();
void end();

template <typename T>
constexpr auto
begin_(T && t) noexcept(noexcept(begin(std::forward<T>(t))))
-> decltype(begin(std::forward<T>(t)))
{
    return begin(std::forward<T>(t));
}

template <typename T>
constexpr auto
end_(T && t) noexcept(noexcept(end(std::forward<T>(t))))
-> decltype(end(std::forward<T>(t)))
{
    return end(std::forward<T>(t));
}

// ----------------------------------------------------------------------------
// from_atlas_cast support utilities
// ----------------------------------------------------------------------------

template <typename FromT, typename ToT>
using forward_like_t = typename std::conditional<
    is_lref<FromT>::value,
    ToT const &,
    ToT &&>::type;

// Drill-to probe: true when TargetT is an actual layer of SourceT's atlas
// chain.  The reference target means the layer must genuinely exist -- a type
// that is merely constructible from what SourceT drills down to does not
// qualify.  The probe uses a const lvalue, so the reference-target rvalue
// rejection does not interfere.
template <typename SourceT, typename TargetT, typename = void>
struct is_atlas_castable : std::false_type {};

template <typename SourceT, typename TargetT>
struct is_atlas_castable<SourceT, TargetT,
    void_t<decltype(cast_impl<TargetT const &>(
        std::declval<remove_cvref_t<SourceT> const &>(), cast_tag{}))>>
: std::true_type {};

// Value-category-honest probe of the exact cast the from_atlas_cast
// constructor body performs (always a value target: lvalue chains drill by
// reference and copy at the boundary, rvalue chains move layer by layer), so
// a construction the lifetime guards would reject SFINAEs out instead of
// reporting viable and then hard-erroring.
template <typename ArgT, typename UnderlyingT, typename = void>
struct is_from_cast_constructible : std::false_type {};

template <typename ArgT, typename UnderlyingT>
struct is_from_cast_constructible<ArgT, UnderlyingT,
    void_t<decltype(cast_impl<UnderlyingT>(
        std::declval<ArgT>(), cast_tag{}))>>
: std::true_type {};

} // namespace atlas_detail

using atlas_detail::enable_if_t;
using atlas_detail::remove_cv_t;
using atlas_detail::remove_cvref_t;
using atlas_detail::when;

template <typename T>
using is_atlas_type = atlas_detail::has_atlas_value_type<T>;

template <typename T>
using holds_enum = atlas_detail::holds_enum_impl<atlas_detail::remove_cvref_t<T>>;

// Remove exactly one layer, at the type level. SFINAE-friendly: not defined
// when T is neither an atlas type nor an enum.
template <typename T>
using unwrap_t = typename atlas_detail::unwrap_type<T>::type;

// Drill to the root type. Identity for non-atlas, non-enum types.
template <typename T>
using undress_t = typename atlas_detail::undress_type<T>::type;

template <typename T, typename U>
struct is_atlas_of
: atlas_detail::bool_c<
    is_atlas_type<T>::value && std::is_same<undress_t<T>, U>::value>
{ };

template <typename T>
struct is_string_atlas
: is_atlas_of<T, std::string>
{ };

template <typename T>
struct is_arithmetic_atlas
: atlas_detail::bool_c<
    is_atlas_type<T>::value &&
    std::is_arithmetic<undress_t<T>>::value &&
    not std::is_same<undress_t<T>, bool>::value>
{ };

#if defined(__cpp_concepts) && __cpp_concepts >= 201907L
template <typename T>
concept AtlasTypeC = is_atlas_type<T>::value;

template <typename T>
concept HoldsEnumC = holds_enum<T>::value;

// Spelled as conjunctions including AtlasTypeC so that the more specific
// concepts subsume it, and an overload constrained on StringAtlasC beats one
// constrained on AtlasTypeC instead of being ambiguous with it.
template <typename T, typename U>
concept AtlasOfC = AtlasTypeC<T> && std::is_same_v<undress_t<T>, U>;

template <typename T>
concept StringAtlasC = AtlasOfC<T, std::string>;

template <typename T>
concept ArithmeticAtlasC = AtlasTypeC<T> &&
    std::is_arithmetic_v<undress_t<T>> &&
    not std::is_same_v<undress_t<T>, bool>;
#endif

#if defined(__cpp_inline_variables) && __cpp_inline_variables >= 201606L
inline constexpr auto undress = atlas_detail::ToUnderlying{};
#elif defined(__cpp_variable_templates) && __cpp_variable_templates >= 201304L
constexpr auto undress = atlas_detail::ToUnderlying{};
#else
// fallback: not nice, but not terrible and prevents ADL
namespace {
constexpr atlas_detail::ToUnderlying undress{};
}
#endif

#if defined(__cpp_inline_variables) && __cpp_inline_variables >= 201606L
inline constexpr auto unwrap = atlas_detail::Unwrap{};
#elif defined(__cpp_variable_templates) && __cpp_variable_templates >= 201304L
constexpr auto unwrap = atlas_detail::Unwrap{};
#else
namespace {
constexpr atlas_detail::Unwrap unwrap{};
}
#endif

#if defined(__cpp_inline_variables) && __cpp_inline_variables >= 201606L
inline constexpr auto undress_enum = atlas_detail::UndressEnum{};
#elif defined(__cpp_variable_templates) && __cpp_variable_templates >= 201304L
constexpr auto undress_enum = atlas_detail::UndressEnum{};
#else
namespace {
constexpr atlas_detail::UndressEnum undress_enum{};
}
#endif

#if defined(__cpp_inline_variables) && __cpp_inline_variables >= 201606L
template <typename TargetT>
inline constexpr atlas_detail::CastTo<TargetT> cast{};
#elif defined(__cpp_variable_templates) && __cpp_variable_templates >= 201304L
template <typename TargetT>
constexpr atlas_detail::CastTo<TargetT> cast{};
#else
// fallback: function template (ADL still possible, but unavoidable in C++11)
template <typename TargetT, typename U>
constexpr auto
cast(U && u)
-> decltype(atlas_detail::cast_impl<TargetT>(
    std::forward<U>(u),
    atlas_detail::cast_tag{}))
{
    return atlas_detail::cast_impl<TargetT>(
        std::forward<U>(u),
        atlas_detail::cast_tag{});
}
#endif

} // namespace atlas

#endif // WJH_ATLAS_50E620B544874CB8BE4412EE6773BF90

// This check runs in every generated header, outside the shared guard: when
// a header generated by an older Atlas is included first, its boilerplate
// claims the guard but lacks pieces this header's inline code needs.  Only
// that order is detectable -- a header generated before this check existed
// carries no check text, so including it after a newer header goes
// undiagnosed (harmless while preamble revisions stay additive).  The real
// remedy either way: regenerate every Atlas header with one Atlas version.
#if !defined(WJH_ATLAS_PREAMBLE_VERSION) || WJH_ATLAS_PREAMBLE_VERSION < 2
#error "An Atlas header generated by an older, incompatible Atlas version was included earlier in this translation unit. Regenerate all Atlas headers with the same Atlas version."
#endif

#ifndef WJH_ATLAS_771333B44A11491895F986933BB2FB41
#define WJH_ATLAS_771333B44A11491895F986933BB2FB41
namespace atlas {
namespace atlas_detail {
// ----------------------------------------------------------------------------
// Hash drilling support
// ----------------------------------------------------------------------------

// is_hashable<T>: detects if std::hash<T> is valid
template <typename T, typename = void>
struct is_hashable
: std::false_type
{ };

template <typename T>
struct is_hashable<
    T,
    void_t<decltype(std::hash<T>{}(std::declval<T const &>()))>>
: std::true_type
{ };

// Base case: T is directly hashable
template <typename T>
auto hash_drill(T const & t, PriorityTag<2>)
-> typename std::enable_if<
    is_hashable<T>::value,
    std::size_t>::type
{
    return std::hash<T>{}(t);
}

// Enum fallback: T is an enum without std::hash, use underlying type
template <typename T>
auto hash_drill(T const & t, PriorityTag<1>)
-> typename std::enable_if<
    std::is_enum<T>::value &&
    not is_hashable<T>::value,
    std::size_t>::type
{
    return std::hash<typename std::underlying_type<T>::type>{}(
        static_cast<typename std::underlying_type<T>::type>(t));
}

// Recursive case: T is an atlas type, drill down
template <typename T>
auto hash_drill(T const & t, PriorityTag<0>)
-> decltype(hash_drill(atlas_value_for(t), PriorityTag<2>{}))
{
    return hash_drill(atlas_value_for(t), PriorityTag<2>{});
}
} // namespace atlas_detail
} // namespace atlas
#endif // WJH_ATLAS_771333B44A11491895F986933BB2FB41

#ifndef WJH_ATLAS_60461ED5AEEF4509B86FB80C8B1E0FE0
#define WJH_ATLAS_60461ED5AEEF4509B86FB80C8B1E0FE0
namespace atlas {
namespace atlas_detail {
// ----------------------------------------------------------------------------
// OStream drilling support
// ----------------------------------------------------------------------------

// is_ostreamable<T>: detects if T can be written to std::ostream
template <typename T, typename = void>
struct is_ostreamable
: std::false_type
{ };

template <typename T>
struct is_ostreamable<
    T,
    void_t<decltype(std::declval<std::ostream &>() << std::declval<T const &>())>>
: std::true_type
{ };

// Base case: T is directly ostreamable
template <typename T>
auto ostream_drill(std::ostream & strm, T const & t, PriorityTag<2>)
-> typename std::enable_if<
    is_ostreamable<T>::value,
    std::ostream &>::type
{
    return strm << t;
}

// Enum fallback: T is an enum without operator<<, use underlying type
template <typename T>
auto ostream_drill(std::ostream & strm, T const & t, PriorityTag<1>)
-> typename std::enable_if<
    std::is_enum<T>::value &&
    not is_ostreamable<T>::value,
    std::ostream &>::type
{
    return strm << static_cast<typename std::underlying_type<T>::type>(t);
}

// Recursive case: T is an atlas type, drill down
template <typename T>
auto ostream_drill(std::ostream & strm, T const & t, PriorityTag<0>)
-> decltype(ostream_drill(strm, atlas_value_for(t), PriorityTag<2>{}))
{
    return ostream_drill(strm, atlas_value_for(t), PriorityTag<2>{});
}
} // namespace atlas_detail
} // namespace atlas
#endif // WJH_ATLAS_60461ED5AEEF4509B86FB80C8B1E0FE0

#ifndef WJH_ATLAS_4296E303C8F846C0B958EB450C57465B
#define WJH_ATLAS_4296E303C8F846C0B958EB450C57465B
namespace atlas {
namespace atlas_detail {
// ----------------------------------------------------------------------------
// IStream drilling support
// ----------------------------------------------------------------------------

// is_istreamable<T>: detects if T can be read from std::istream
template <typename T, typename = void>
struct is_istreamable
: std::false_type
{ };

template <typename T>
struct is_istreamable<
    T,
    void_t<decltype(std::declval<std::istream &>() >> std::declval<T &>())>>
: std::true_type
{ };

// Base case: T is directly istreamable
template <typename T>
auto istream_drill(std::istream & strm, T & t, PriorityTag<2>)
-> typename std::enable_if<
    is_istreamable<T>::value,
    std::istream &>::type
{
    return strm >> t;
}

// Enum fallback: T is an enum without operator>>, read as underlying type
template <typename T>
auto istream_drill(std::istream & strm, T & t, PriorityTag<1>)
-> typename std::enable_if<
    std::is_enum<T>::value &&
    not is_istreamable<T>::value,
    std::istream &>::type
{
    typename std::underlying_type<T>::type tmp;
    strm >> tmp;
    t = static_cast<T>(tmp);
    return strm;
}

// Recursive case: T is an atlas type, drill down
template <typename T>
auto istream_drill(std::istream & strm, T & t, PriorityTag<0>)
-> decltype(istream_drill(strm, atlas_value_for(t), PriorityTag<2>{}))
{
    return istream_drill(strm, atlas_value_for(t), PriorityTag<2>{});
}
} // namespace atlas_detail
} // namespace atlas
#endif // WJH_ATLAS_4296E303C8F846C0B958EB450C57465B

#ifndef WJH_ATLAS_9B74AE244B4F4EB68DF9D80B67E1EB05
#define WJH_ATLAS_9B74AE244B4F4EB68DF9D80B67E1EB05
namespace atlas {
namespace atlas_detail {
// ----------------------------------------------------------------------------
// Format drilling support (C++20+)
// ----------------------------------------------------------------------------
#if defined(__cpp_lib_format) && __cpp_lib_format >= 202110L

// Concept: T is formattable via std::formatter<T>
template <typename T>
concept formattable = requires(
    std::formatter<T> f,
    T const & t,
    std::format_parse_context & parse_ctx,
    std::format_context & fmt_ctx)
{
    f.parse(parse_ctx);
    f.format(t, fmt_ctx);
};

// Drill to find a formattable type, returning a reference or converted value
template <typename T>
constexpr decltype(auto) format_value_drill(T const & t)
{
    if constexpr (formattable<T>) {
        // Base case: T is directly formattable - return reference
        return (t);
    } else if constexpr (std::is_enum_v<T>) {
        // Enum fallback: convert to underlying type
        return static_cast<std::underlying_type_t<T>>(t);
    } else if constexpr (has_atlas_value_type<T>::value) {
        // Recursive case: drill through atlas type
        return format_value_drill(atlas_value_for(t));
    } else {
        static_assert(formattable<T>, "Type is not formattable after drilling");
    }
}

// Type trait for the drilled type
template <typename T>
using format_drilled_type_t =
    std::remove_cvref_t<decltype(format_value_drill(std::declval<T const &>()))>;

#endif // __cpp_lib_format
} // namespace atlas_detail
} // namespace atlas
#endif // WJH_ATLAS_9B74AE244B4F4EB68DF9D80B67E1EB05

#ifndef WJH_ATLAS_173D2C4FC9AA46929AD14C8BDF75D829
#define WJH_ATLAS_173D2C4FC9AA46929AD14C8BDF75D829

#include <sstream>

#ifdef __clang__
    #pragma clang diagnostic push
    #pragma clang diagnostic ignored "-Wweak-vtables"
#endif

namespace atlas {

/**
 * @brief Exception thrown when a constraint is violated
 */
class ConstraintError
: public std::logic_error
{
public:
    using std::logic_error::logic_error;
};

namespace constraints {

namespace detail {

template <typename T>
std::string
format_value_impl(T const &, atlas_detail::PriorityTag<0>)
{
    return "unknown value";
}

template <typename T>
auto
format_value_impl(T const & value, atlas_detail::PriorityTag<1>)
-> decltype(std::declval<std::ostringstream &>() << value, std::string())
{
    std::ostringstream oss;
    oss << value;
    return oss.str();
}

template <typename T, atlas_detail::when<std::is_arithmetic<T>::value> = true>
std::string
format_value_impl(T const & value, atlas_detail::PriorityTag<2>)
{
    using U = typename std::conditional<
        std::is_integral<T>::value && sizeof(T) < sizeof(int),
        typename std::conditional<
            std::is_unsigned<T>::value,
            unsigned int,
            signed int>::type,
        T>::type;
    return std::to_string(static_cast<U>(value));
}

template <typename T>
std::string
format_value(T const & value)
{
    return format_value_impl(value, atlas_detail::PriorityTag<2>{});
}

inline int uncaught_exceptions() noexcept
{
#if defined(__cpp_lib_uncaught_exceptions) && \
    __cpp_lib_uncaught_exceptions >= 201411L
    return std::uncaught_exceptions();
#elif defined(_MSC_VER)
    return __uncaught_exceptions();  // MSVC extension available since VS2015
#elif defined(__GLIBCXX__)
    // libstdc++ has __cxa_get_globals which tracks uncaught exceptions
    return __cxxabiv1::__cxa_get_globals()->uncaughtExceptions;
#elif defined(_LIBCPP_VERSION)
    // libc++ has std::uncaught_exceptions even in C++11 mode as extension
    return std::uncaught_exceptions();
#else
    // Fallback: use old uncaught_exception() (singular) - less safe but works
    // This will return 1 during any exception, 0 otherwise
    // Can't distinguish between multiple exceptions, but better than nothing
    return std::uncaught_exception() ? 1 : 0;
#endif
}

/**
 * @brief RAII guard for validating constraints after mutating operations
 *
 * This guard validates constraints in its destructor, ensuring that the
 * constraint is checked after the operation completes. The guard checks
 * uncaught_exceptions() to avoid throwing during stack unwinding.
 *
 * Only validates non-const operations - const operations cannot violate
 * constraints by definition.
 *
 * @tparam T The value type being constrained (may be const)
 * @tparam ConstraintT The constraint type with static check() and message()
 */
template <typename T, typename ConstraintT, typename = void>
struct ConstraintGuard
{
    using value_type = typename std::remove_const<T>::type;

    T const & value;
    char const * operation_name;
    int uncaught_at_entry;

    /**
     * @brief Construct guard, capturing current exception state
     */
    constexpr ConstraintGuard(T const & v, char const * op) noexcept
    : value(v)
    , operation_name(op)
    , uncaught_at_entry(uncaught_exceptions())
    { }

    /**
     * @brief Destructor validates constraint if no new exceptions
     *
     * Only throws if the constraint is violated AND no exceptions are
     * currently unwinding (to avoid std::terminate).
     *
     * Only validates non-const operations - uses std::is_const to check.
     */
    constexpr ~ConstraintGuard() noexcept(false)
    {
        if (uncaught_exceptions() == uncaught_at_entry) {
            if (not ConstraintT::check(value)) {
                throw atlas::ConstraintError(
                    std::string(operation_name) +
                    ": operation violates constraint (" +
                    ConstraintT::message() + ")");
            }
        }
    }
};

template <typename T, typename ConstraintT>
struct ConstraintGuard<
    T,
    ConstraintT,
    typename std::enable_if<std::is_const<T>::value>::type>
{
    constexpr ConstraintGuard(T const &, char const *) noexcept
    { }
};

} // namespace detail

template <typename ConstraintT, typename T>
auto constraint_guard(T & t, char const * op) noexcept
{
    return detail::ConstraintGuard<T, ConstraintT>(t, op);
}

template <typename T>
constexpr auto is_nil_value(typename T::atlas_value_type const * value)
-> decltype(atlas::undress(T::nil_value) == *value)
{
    return atlas::undress(T::nil_value) == *value;
}

template <typename T>
constexpr bool is_nil_value(void const *)
{
    return false;
}

template <typename T>
constexpr bool check(typename T::atlas_value_type const & value)
{
    return is_nil_value<T>(std::addressof(value)) ||
        T::atlas_constraint::check(value);
}

/**
 * @brief Constraint: value must be > 0
 */
template <typename T>
struct positive
{
    static constexpr bool check(T const & value)
    noexcept(noexcept(value > T{0}))
    {
        return value > T{0};
    }

    static constexpr char const * message() noexcept
    {
        return "value must be positive (> 0)";
    }
};

/**
 * @brief Constraint: value must be >= 0
 */
template <typename T>
struct non_negative
{
    static constexpr bool check(T const & value)
    noexcept(noexcept(value >= T{0}))
    {
        return value >= T{0};
    }

    static constexpr char const * message() noexcept
    {
        return "value must be non-negative (>= 0)";
    }
};

/**
 * @brief Constraint: value must be != 0
 */
template <typename T>
struct non_zero
{
    static constexpr bool check(T const & value)
    noexcept(noexcept(value != T{0}))
    {
        return value != T{0};
    }

    static constexpr char const * message() noexcept
    {
        return "value must be non-zero (!= 0)";
    }
};

/**
 * Constraint: value must be in [Min, Max]
 */
template <typename T>
struct bounded
{
    static constexpr bool check(typename T::value_type const & value)
    noexcept(noexcept(value >= T::min()) && noexcept(value <= T::max()))
    {
        return value >= T::min() && value <= T::max();
    }

    static constexpr char const * message() noexcept
    {
        return T::message();
    }
};

/**
 * Constraint: value must be in [Min, Max) (half-open range)
 */
template <typename T>
struct bounded_range
{
    static constexpr bool check(typename T::value_type const & value)
    noexcept(noexcept(value >= T::min()) && noexcept(value < T::max()))
    {
        return value >= T::min() && value < T::max();
    }

    static constexpr char const * message() noexcept
    {
        return T::message();
    }
};

/**
 * @brief Constraint: container/string must not be empty
 */
template <typename T>
struct non_empty
{
    static constexpr bool check(T const & value)
    noexcept(noexcept(value.empty()))
    {
        return not value.empty();
    }

    static constexpr char const * message() noexcept
    {
        return "value must not be empty";
    }
};

/**
 * @brief Constraint: pointer must not be null
 *
 * Works with raw pointers, smart pointers (unique_ptr, shared_ptr), and
 * std::optional by using explicit bool conversion (operator bool()).
 *
 * Note: weak_ptr requires C++23 for operator bool() support.
 */
template <typename T>
struct non_null
{
    static constexpr bool check(T const & value)
    noexcept(noexcept(static_cast<bool>(value)))
    {
        // Use explicit bool conversion - works for:
        // - Raw pointers (void*, int*, etc.)
        // - Smart pointers (unique_ptr, shared_ptr)
        // - std::optional
        // - Any type with explicit operator bool()
        return static_cast<bool>(value);
    }

    static constexpr char const * message() noexcept
    {
        return "pointer must not be null";
    }
};

} // namespace constraints
} // namespace atlas

#ifdef __clang__
    #pragma clang diagnostic pop
#endif

#endif // WJH_ATLAS_173D2C4FC9AA46929AD14C8BDF75D829

#ifndef WJH_ATLAS_83B11BF12B6945019DF71C7517A1D6DA
#define WJH_ATLAS_83B11BF12B6945019DF71C7517A1D6DA
#if __cplusplus >= 202002L
namespace atlas::atlas_detail {

// Concept: T is an atlas type whose underlying value chain is eventually
// hashable
template <typename T>
concept atlas_hashable = has_atlas_value_type<T>::value &&
    requires(T const & t) {
        { hash_drill(atlas_value_for(t), PriorityTag<2>{}) }
            -> std::same_as<std::size_t>;
    };

} // namespace atlas::atlas_detail

template <typename T>
    requires atlas::atlas_detail::atlas_hashable<T>
struct std::hash<T>
{
    std::size_t operator()(T const & t) const
    noexcept(noexcept(atlas::atlas_detail::hash_drill(
        atlas_value_for(t), atlas::atlas_detail::PriorityTag<2>{})))
    {
        return atlas::atlas_detail::hash_drill(
            atlas_value_for(t), atlas::atlas_detail::PriorityTag<2>{});
    }
};
#else // pre-C++20
namespace atlas {
namespace atlas_detail {

// Base class for per-type std::hash specializations.
// The template <typename U = T> trick defers instantiation of the
// return type until operator() is actually called, providing a
// SFINAE-like effect for most cases. Note: this relies on hash_drill
// producing a substitution failure (not a hard error) for non-hashable
// types, which holds for all atlas-generated types.
template <typename T>
struct hash_base
{
    template <typename U = T>
    ATLAS_NODISCARD
    auto operator()(U const & t) const
    noexcept(noexcept(hash_drill(
        atlas_value_for(t), PriorityTag<2>{})))
    -> decltype(hash_drill(
        atlas_value_for(t), PriorityTag<2>{}))
    {
        return hash_drill(
            atlas_value_for(t), PriorityTag<2>{});
    }
};

} // namespace atlas_detail
} // namespace atlas
#endif // C++20
#endif // WJH_ATLAS_83B11BF12B6945019DF71C7517A1D6DA

#ifndef WJH_ATLAS_A3A2ADA707CA47BE9EB94254C729C906
#define WJH_ATLAS_A3A2ADA707CA47BE9EB94254C729C906
#if defined(__cpp_lib_format) && __cpp_lib_format >= 202110L
namespace atlas::atlas_detail {

// Concept: T is an atlas type whose underlying value chain is eventually
// formattable
template <typename T>
concept atlas_formattable = has_atlas_value_type<T>::value &&
    requires(T const & t) {
        format_value_drill(atlas_value_for(t));
    };

} // namespace atlas::atlas_detail

template <typename T>
    requires atlas::atlas_detail::atlas_formattable<T>
struct std::formatter<T>
{
private:
    using drilled_type_ =
        atlas::atlas_detail::format_drilled_type_t<typename T::atlas_value_type>;
    std::formatter<drilled_type_> underlying_formatter_;

public:
    constexpr auto parse(std::format_parse_context & ctx)
    {
        return underlying_formatter_.parse(ctx);
    }

    auto format(T const & t, std::format_context & ctx) const
    {
        return underlying_formatter_.format(
            atlas::atlas_detail::format_value_drill(atlas_value_for(t)),
            ctx);
    }
};
#endif // __cpp_lib_format
#endif // WJH_ATLAS_A3A2ADA707CA47BE9EB94254C729C906

#ifndef WJH_ATLAS_204DEDF8AD1A4B8EA2910C659C4523BC
#define WJH_ATLAS_204DEDF8AD1A4B8EA2910C659C4523BC
namespace atlas {

// Templated operator<< for all atlas types
// ADL finds this via atlas::strong_type_tag base class
template <typename T>
auto operator<<(std::ostream & strm, T const & t)
-> typename std::enable_if<
    atlas_detail::has_atlas_value_type<T>::value,
    decltype(atlas_detail::ostream_drill(
        strm, atlas_value_for(t), atlas_detail::PriorityTag<2>{}))>::type
{
    return atlas_detail::ostream_drill(
        strm, atlas_value_for(t), atlas_detail::PriorityTag<2>{});
}

} // namespace atlas
#endif // WJH_ATLAS_204DEDF8AD1A4B8EA2910C659C4523BC

#ifndef WJH_ATLAS_8E1585765002403FBFA3B92531F0A25E
#define WJH_ATLAS_8E1585765002403FBFA3B92531F0A25E
namespace atlas {

// Templated operator>> for all atlas types
// ADL finds this via atlas::strong_type_tag base class
template <typename T>
auto operator>>(std::istream & strm, T & t)
-> typename std::enable_if<
    atlas_detail::has_atlas_value_type<T>::value,
    decltype(atlas_detail::istream_drill(
        strm, atlas_value_for(t), atlas_detail::PriorityTag<2>{}))>::type
{
    return atlas_detail::istream_drill(
        strm, atlas_value_for(t), atlas_detail::PriorityTag<2>{});
}

} // namespace atlas
#endif // WJH_ATLAS_8E1585765002403FBFA3B92531F0A25E


//////////////////////////////////////////////////////////////////////
///
/// These are the droids you are looking for!
///
//////////////////////////////////////////////////////////////////////


namespace wjh {
namespace chat {

/**
 * @brief Strong type wrapper for std::string
 *
 * Generated by Atlas Strong Type Generator.
 * Generation parameters:
 * - kind: class
 * - type_namespace: wjh::chat
 * - type_name: ApiKey
 * - description: std::string; <=>, non_empty
 * - default_value: ""
 */
class ApiKey
: private atlas::strong_type_tag<ApiKey>
{
    std::string value;

public:
    using atlas_value_type = std::string;
    using atlas_constraint = atlas::constraints::non_empty<std::string>;

    ApiKey() = delete;

    template <
        typename... ArgTs,
        typename std::enable_if<
            std::is_constructible<std::string, ArgTs...>::value,
            bool>::type = true>
    constexpr explicit ApiKey(ArgTs && ... args)
    : value(std::forward<ArgTs>(args)...)
    {
        if (not atlas::constraints::check<ApiKey>(value)) {
            throw atlas::ConstraintError(
                "ApiKey: " +
                atlas::constraints::detail::format_value(value) +
                " violates constraint: value must not be empty");
        }
    }

    /**
     * Access to immediate underlying value via ADL.
     */
    friend constexpr std::string const & atlas_value_for(ApiKey const & self) noexcept {
        return self.value;
    }
    friend constexpr std::string & atlas_value_for(ApiKey & self) noexcept {
        return self.value;
    }
    friend constexpr auto atlas_value_for(ApiKey && self) noexcept
        -> typename std::enable_if<
            std::is_move_constructible<std::string>::value,
            std::string>::type
    {
        return std::move(self.value);
    }
    friend constexpr auto atlas_value_for(
        ApiKey && self,
        atlas::atlas_detail::ref_tag) noexcept
        -> std::string &&
    {
        return static_cast<std::string &&>(self.value);
    }

    /**
     * Lifetime guard: converting an expiring object to a type that borrows
     * its storage would dangle.  Deleted, non-explicit, and rvalue-only, so
     * every conversion route from a temporary -- static_cast,
     * copy-initialization, and implicit conversion -- is rejected at compile
     * time.  Inert (never a candidate) unless the target borrows and the
     * underlying type owns; specialize atlas::borrow_policy to adjust.
     * The remedy at a rejected call site is to bind the source to a named
     * variable first.
     */
    template <
        typename BorrowTargetT,
        typename = atlas::atlas_detail::enable_if_t<
            atlas::borrow_policy<BorrowTargetT, std::string>::value>>
    operator BorrowTargetT () const &&
#if defined(__cpp_deleted_function) && __cpp_deleted_function >= 202403L \
    && __cplusplus > 202302L
    = delete("converting an expiring object to a type that borrows its "
        "storage dangles; bind the source to a named variable first");
#else
    = delete;
#endif

    /**
     * @brief Forward substr to wrapped object
     *
     * This member function forwards all calls to the underlying type's
     * substr member function, preserving const-correctness,
     * noexcept specifications, and perfect forwarding.
     *
     * IMPORTANT: Constraint checking occurs AFTER the operation executes.
     * This is an inherent limitation of generic constraint checking - we cannot
     * know ahead of time if an operation will violate a constraint without
     * operation-specific knowledge.
     */
#if defined(__cpp_explicit_this_parameter) && __cpp_explicit_this_parameter >= 202110L
    // C++23 deducing this - single elegant overload
    template <typename Self, typename... Args>
    constexpr auto substr(this Self&& self, Args&&... args)
    -> decltype(std::forward<Self>(self).value.substr(std::forward<Args>(args)...))
    {
        using atlas::constraints::constraint_guard;
        [[maybe_unused]] auto guard = constraint_guard<atlas_constraint>(
            self.value,
            "ApiKey::substr");
        return std::forward<Self>(self).value.substr(std::forward<Args>(args)...);
    }
#else
    // C++11-20: ref-qualified overloads (or just const for const-only)

    template <typename... Args>
    constexpr auto substr(Args&&... args) const &
    noexcept(noexcept(value.substr(std::forward<Args>(args)...)))
    -> decltype(value.substr(std::forward<Args>(args)...))
    {
        return value.substr(std::forward<Args>(args)...);
    }

    template <typename... Args>
    constexpr auto substr(Args&&... args) const &&
    noexcept(noexcept(std::move(value).substr(std::forward<Args>(args)...)))
    -> decltype(std::move(value).substr(std::forward<Args>(args)...))
    {
        return std::move(value).substr(std::forward<Args>(args)...);
    }

    template <typename... Args>
    constexpr auto substr(Args&&... args) &
    -> decltype(value.substr(std::forward<Args>(args)...))
    {
        using atlas::constraints::constraint_guard;
        [[maybe_unused]] auto guard = constraint_guard<atlas_constraint>(
            value,
            "ApiKey::substr");
        return value.substr(std::forward<Args>(args)...);
    }

    template <typename... Args>
    constexpr auto substr(Args&&... args) &&
    -> decltype(std::move(value).substr(std::forward<Args>(args)...))
    {
        using atlas::constraints::constraint_guard;
        [[maybe_unused]] auto guard = constraint_guard<atlas_constraint>(
            value,
            "ApiKey::substr");
        return std::move(value).substr(std::forward<Args>(args)...);
    }
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default three-way comparison (spaceship) operator.
     */
    friend constexpr auto operator <=> (
        ApiKey const &,
        ApiKey const &) = default;
#else
    /**
     * Comparison operators (C++17 fallback for spaceship operator).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator < (
        ApiKey const & lhs,
        ApiKey const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() <
        std::declval<std::string const &>()))
    {
        return lhs.value < rhs.value;
    }

    friend constexpr bool operator <= (
        ApiKey const & lhs,
        ApiKey const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() <=
        std::declval<std::string const &>()))
    {
        return lhs.value <= rhs.value;
    }

    friend constexpr bool operator > (
        ApiKey const & lhs,
        ApiKey const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() >
        std::declval<std::string const &>()))
    {
        return lhs.value > rhs.value;
    }

    friend constexpr bool operator >= (
        ApiKey const & lhs,
        ApiKey const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() >=
        std::declval<std::string const &>()))
    {
        return lhs.value >= rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default equality comparison operator.
     * Provided with spaceship operator for optimal performance.
     */
    friend constexpr bool operator == (
        ApiKey const &,
        ApiKey const &) = default;
#else
    /**
     * Equality comparison operators (C++17 fallback).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator == (
        ApiKey const & lhs,
        ApiKey const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() ==
        std::declval<std::string const &>()))
    {
        return lhs.value == rhs.value;
    }

    friend constexpr bool operator != (
        ApiKey const & lhs,
        ApiKey const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() !=
        std::declval<std::string const &>()))
    {
        return lhs.value != rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
};
} // namespace chat
} // namespace wjh


#if __cplusplus < 202002L
template <>
struct std::hash<wjh::chat::ApiKey>
: atlas::atlas_detail::hash_base<wjh::chat::ApiKey>
{};
#endif

namespace wjh {
namespace chat {

/**
 * @brief Strong type wrapper for std::string
 *
 * Generated by Atlas Strong Type Generator.
 * Generation parameters:
 * - kind: class
 * - type_namespace: wjh::chat
 * - type_name: ModelId
 * - description: std::string; <=>, non_empty
 * - default_value: ""anthropic/claude-sonnet-4""
 */
class ModelId
: private atlas::strong_type_tag<ModelId>
{
    std::string value = static_cast<std::string>("anthropic/claude-sonnet-4");

public:
    using atlas_value_type = std::string;
    using atlas_constraint = atlas::constraints::non_empty<std::string>;

    ModelId() = delete;

    template <
        typename... ArgTs,
        typename std::enable_if<
            std::is_constructible<std::string, ArgTs...>::value,
            bool>::type = true>
    constexpr explicit ModelId(ArgTs && ... args)
    : value(std::forward<ArgTs>(args)...)
    {
        if (not atlas::constraints::check<ModelId>(value)) {
            throw atlas::ConstraintError(
                "ModelId: " +
                atlas::constraints::detail::format_value(value) +
                " violates constraint: value must not be empty");
        }
    }

    /**
     * Access to immediate underlying value via ADL.
     */
    friend constexpr std::string const & atlas_value_for(ModelId const & self) noexcept {
        return self.value;
    }
    friend constexpr std::string & atlas_value_for(ModelId & self) noexcept {
        return self.value;
    }
    friend constexpr auto atlas_value_for(ModelId && self) noexcept
        -> typename std::enable_if<
            std::is_move_constructible<std::string>::value,
            std::string>::type
    {
        return std::move(self.value);
    }
    friend constexpr auto atlas_value_for(
        ModelId && self,
        atlas::atlas_detail::ref_tag) noexcept
        -> std::string &&
    {
        return static_cast<std::string &&>(self.value);
    }

    /**
     * Lifetime guard: converting an expiring object to a type that borrows
     * its storage would dangle.  Deleted, non-explicit, and rvalue-only, so
     * every conversion route from a temporary -- static_cast,
     * copy-initialization, and implicit conversion -- is rejected at compile
     * time.  Inert (never a candidate) unless the target borrows and the
     * underlying type owns; specialize atlas::borrow_policy to adjust.
     * The remedy at a rejected call site is to bind the source to a named
     * variable first.
     */
    template <
        typename BorrowTargetT,
        typename = atlas::atlas_detail::enable_if_t<
            atlas::borrow_policy<BorrowTargetT, std::string>::value>>
    operator BorrowTargetT () const &&
#if defined(__cpp_deleted_function) && __cpp_deleted_function >= 202403L \
    && __cplusplus > 202302L
    = delete("converting an expiring object to a type that borrows its "
        "storage dangles; bind the source to a named variable first");
#else
    = delete;
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default three-way comparison (spaceship) operator.
     */
    friend constexpr auto operator <=> (
        ModelId const &,
        ModelId const &) = default;
#else
    /**
     * Comparison operators (C++17 fallback for spaceship operator).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator < (
        ModelId const & lhs,
        ModelId const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() <
        std::declval<std::string const &>()))
    {
        return lhs.value < rhs.value;
    }

    friend constexpr bool operator <= (
        ModelId const & lhs,
        ModelId const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() <=
        std::declval<std::string const &>()))
    {
        return lhs.value <= rhs.value;
    }

    friend constexpr bool operator > (
        ModelId const & lhs,
        ModelId const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() >
        std::declval<std::string const &>()))
    {
        return lhs.value > rhs.value;
    }

    friend constexpr bool operator >= (
        ModelId const & lhs,
        ModelId const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() >=
        std::declval<std::string const &>()))
    {
        return lhs.value >= rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default equality comparison operator.
     * Provided with spaceship operator for optimal performance.
     */
    friend constexpr bool operator == (
        ModelId const &,
        ModelId const &) = default;
#else
    /**
     * Equality comparison operators (C++17 fallback).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator == (
        ModelId const & lhs,
        ModelId const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() ==
        std::declval<std::string const &>()))
    {
        return lhs.value == rhs.value;
    }

    friend constexpr bool operator != (
        ModelId const & lhs,
        ModelId const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() !=
        std::declval<std::string const &>()))
    {
        return lhs.value != rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
};
} // namespace chat
} // namespace wjh


#if __cplusplus < 202002L
template <>
struct std::hash<wjh::chat::ModelId>
: atlas::atlas_detail::hash_base<wjh::chat::ModelId>
{};
#endif

namespace wjh {
namespace chat {

/**
 * @brief Strong type wrapper for std::uint32_t
 *
 * Generated by Atlas Strong Type Generator.
 * Generation parameters:
 * - kind: class
 * - type_namespace: wjh::chat
 * - type_name: MaxTokens
 * - description: std::uint32_t; <=>, positive
 * - default_value: "4096u"
 */
class MaxTokens
: private atlas::strong_type_tag<MaxTokens>
{
    std::uint32_t value = static_cast<std::uint32_t>(4096u);

public:
    using atlas_value_type = std::uint32_t;
    using atlas_constraint = atlas::constraints::positive<std::uint32_t>;

    constexpr explicit MaxTokens() = default;

    template <
        typename... ArgTs,
        typename std::enable_if<
            std::is_constructible<std::uint32_t, ArgTs...>::value,
            bool>::type = true>
    constexpr explicit MaxTokens(ArgTs && ... args)
    : value(std::forward<ArgTs>(args)...)
    {
        if (not atlas::constraints::check<MaxTokens>(value)) {
            throw atlas::ConstraintError(
                "MaxTokens: " +
                atlas::constraints::detail::format_value(value) +
                " violates constraint: value must be positive (> 0)");
        }
    }

    /**
     * Access to immediate underlying value via ADL.
     */
    friend constexpr std::uint32_t const & atlas_value_for(MaxTokens const & self) noexcept {
        return self.value;
    }
    friend constexpr std::uint32_t & atlas_value_for(MaxTokens & self) noexcept {
        return self.value;
    }
    friend constexpr auto atlas_value_for(MaxTokens && self) noexcept
        -> typename std::enable_if<
            std::is_move_constructible<std::uint32_t>::value,
            std::uint32_t>::type
    {
        return std::move(self.value);
    }
    friend constexpr auto atlas_value_for(
        MaxTokens && self,
        atlas::atlas_detail::ref_tag) noexcept
        -> std::uint32_t &&
    {
        return static_cast<std::uint32_t &&>(self.value);
    }

    /**
     * Lifetime guard: converting an expiring object to a type that borrows
     * its storage would dangle.  Deleted, non-explicit, and rvalue-only, so
     * every conversion route from a temporary -- static_cast,
     * copy-initialization, and implicit conversion -- is rejected at compile
     * time.  Inert (never a candidate) unless the target borrows and the
     * underlying type owns; specialize atlas::borrow_policy to adjust.
     * The remedy at a rejected call site is to bind the source to a named
     * variable first.
     */
    template <
        typename BorrowTargetT,
        typename = atlas::atlas_detail::enable_if_t<
            atlas::borrow_policy<BorrowTargetT, std::uint32_t>::value>>
    operator BorrowTargetT () const &&
#if defined(__cpp_deleted_function) && __cpp_deleted_function >= 202403L \
    && __cplusplus > 202302L
    = delete("converting an expiring object to a type that borrows its "
        "storage dangles; bind the source to a named variable first");
#else
    = delete;
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default three-way comparison (spaceship) operator.
     */
    friend constexpr auto operator <=> (
        MaxTokens const &,
        MaxTokens const &) = default;
#else
    /**
     * Comparison operators (C++17 fallback for spaceship operator).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator < (
        MaxTokens const & lhs,
        MaxTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() <
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value < rhs.value;
    }

    friend constexpr bool operator <= (
        MaxTokens const & lhs,
        MaxTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() <=
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value <= rhs.value;
    }

    friend constexpr bool operator > (
        MaxTokens const & lhs,
        MaxTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() >
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value > rhs.value;
    }

    friend constexpr bool operator >= (
        MaxTokens const & lhs,
        MaxTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() >=
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value >= rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default equality comparison operator.
     * Provided with spaceship operator for optimal performance.
     */
    friend constexpr bool operator == (
        MaxTokens const &,
        MaxTokens const &) = default;
#else
    /**
     * Equality comparison operators (C++17 fallback).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator == (
        MaxTokens const & lhs,
        MaxTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() ==
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value == rhs.value;
    }

    friend constexpr bool operator != (
        MaxTokens const & lhs,
        MaxTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() !=
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value != rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
};
} // namespace chat
} // namespace wjh


#if __cplusplus < 202002L
template <>
struct std::hash<wjh::chat::MaxTokens>
: atlas::atlas_detail::hash_base<wjh::chat::MaxTokens>
{};
#endif

namespace wjh {
namespace chat {

/**
 * @brief Strong type wrapper for float
 *
 * Generated by Atlas Strong Type Generator.
 * Generation parameters:
 * - kind: class
 * - type_namespace: wjh::chat
 * - type_name: Temperature
 * - description: float;
 * - default_value: ""
 */
class Temperature
: private atlas::strong_type_tag<Temperature>
{
    float value;

public:
    using atlas_value_type = float;

    constexpr explicit Temperature() = default;

    template <
        typename... ArgTs,
        typename std::enable_if<
            std::is_constructible<float, ArgTs...>::value,
            bool>::type = true>
    constexpr explicit Temperature(ArgTs && ... args)
    : value(std::forward<ArgTs>(args)...)
    { }

    /**
     * Access to immediate underlying value via ADL.
     */
    friend constexpr float const & atlas_value_for(Temperature const & self) noexcept {
        return self.value;
    }
    friend constexpr float & atlas_value_for(Temperature & self) noexcept {
        return self.value;
    }
    friend constexpr auto atlas_value_for(Temperature && self) noexcept
        -> typename std::enable_if<
            std::is_move_constructible<float>::value,
            float>::type
    {
        return std::move(self.value);
    }
    friend constexpr auto atlas_value_for(
        Temperature && self,
        atlas::atlas_detail::ref_tag) noexcept
        -> float &&
    {
        return static_cast<float &&>(self.value);
    }

    /**
     * Lifetime guard: converting an expiring object to a type that borrows
     * its storage would dangle.  Deleted, non-explicit, and rvalue-only, so
     * every conversion route from a temporary -- static_cast,
     * copy-initialization, and implicit conversion -- is rejected at compile
     * time.  Inert (never a candidate) unless the target borrows and the
     * underlying type owns; specialize atlas::borrow_policy to adjust.
     * The remedy at a rejected call site is to bind the source to a named
     * variable first.
     */
    template <
        typename BorrowTargetT,
        typename = atlas::atlas_detail::enable_if_t<
            atlas::borrow_policy<BorrowTargetT, float>::value>>
    operator BorrowTargetT () const &&
#if defined(__cpp_deleted_function) && __cpp_deleted_function >= 202403L \
    && __cplusplus > 202302L
    = delete("converting an expiring object to a type that borrows its "
        "storage dangles; bind the source to a named variable first");
#else
    = delete;
#endif
};
} // namespace chat
} // namespace wjh


#if __cplusplus < 202002L
template <>
struct std::hash<wjh::chat::Temperature>
: atlas::atlas_detail::hash_base<wjh::chat::Temperature>
{};
#endif

namespace wjh {
namespace chat {

/**
 * @brief Strong type wrapper for std::string
 *
 * Generated by Atlas Strong Type Generator.
 * Generation parameters:
 * - kind: class
 * - type_namespace: wjh::chat
 * - type_name: SystemPrompt
 * - description: std::string; <=>
 * - default_value: ""
 */
class SystemPrompt
: private atlas::strong_type_tag<SystemPrompt>
{
    std::string value;

public:
    using atlas_value_type = std::string;

    constexpr explicit SystemPrompt() = default;

    template <
        typename... ArgTs,
        typename std::enable_if<
            std::is_constructible<std::string, ArgTs...>::value,
            bool>::type = true>
    constexpr explicit SystemPrompt(ArgTs && ... args)
    : value(std::forward<ArgTs>(args)...)
    { }

    /**
     * Access to immediate underlying value via ADL.
     */
    friend constexpr std::string const & atlas_value_for(SystemPrompt const & self) noexcept {
        return self.value;
    }
    friend constexpr std::string & atlas_value_for(SystemPrompt & self) noexcept {
        return self.value;
    }
    friend constexpr auto atlas_value_for(SystemPrompt && self) noexcept
        -> typename std::enable_if<
            std::is_move_constructible<std::string>::value,
            std::string>::type
    {
        return std::move(self.value);
    }
    friend constexpr auto atlas_value_for(
        SystemPrompt && self,
        atlas::atlas_detail::ref_tag) noexcept
        -> std::string &&
    {
        return static_cast<std::string &&>(self.value);
    }

    /**
     * Lifetime guard: converting an expiring object to a type that borrows
     * its storage would dangle.  Deleted, non-explicit, and rvalue-only, so
     * every conversion route from a temporary -- static_cast,
     * copy-initialization, and implicit conversion -- is rejected at compile
     * time.  Inert (never a candidate) unless the target borrows and the
     * underlying type owns; specialize atlas::borrow_policy to adjust.
     * The remedy at a rejected call site is to bind the source to a named
     * variable first.
     */
    template <
        typename BorrowTargetT,
        typename = atlas::atlas_detail::enable_if_t<
            atlas::borrow_policy<BorrowTargetT, std::string>::value>>
    operator BorrowTargetT () const &&
#if defined(__cpp_deleted_function) && __cpp_deleted_function >= 202403L \
    && __cplusplus > 202302L
    = delete("converting an expiring object to a type that borrows its "
        "storage dangles; bind the source to a named variable first");
#else
    = delete;
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default three-way comparison (spaceship) operator.
     */
    friend constexpr auto operator <=> (
        SystemPrompt const &,
        SystemPrompt const &) = default;
#else
    /**
     * Comparison operators (C++17 fallback for spaceship operator).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator < (
        SystemPrompt const & lhs,
        SystemPrompt const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() <
        std::declval<std::string const &>()))
    {
        return lhs.value < rhs.value;
    }

    friend constexpr bool operator <= (
        SystemPrompt const & lhs,
        SystemPrompt const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() <=
        std::declval<std::string const &>()))
    {
        return lhs.value <= rhs.value;
    }

    friend constexpr bool operator > (
        SystemPrompt const & lhs,
        SystemPrompt const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() >
        std::declval<std::string const &>()))
    {
        return lhs.value > rhs.value;
    }

    friend constexpr bool operator >= (
        SystemPrompt const & lhs,
        SystemPrompt const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() >=
        std::declval<std::string const &>()))
    {
        return lhs.value >= rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default equality comparison operator.
     * Provided with spaceship operator for optimal performance.
     */
    friend constexpr bool operator == (
        SystemPrompt const &,
        SystemPrompt const &) = default;
#else
    /**
     * Equality comparison operators (C++17 fallback).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator == (
        SystemPrompt const & lhs,
        SystemPrompt const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() ==
        std::declval<std::string const &>()))
    {
        return lhs.value == rhs.value;
    }

    friend constexpr bool operator != (
        SystemPrompt const & lhs,
        SystemPrompt const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() !=
        std::declval<std::string const &>()))
    {
        return lhs.value != rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
};
} // namespace chat
} // namespace wjh


#if __cplusplus < 202002L
template <>
struct std::hash<wjh::chat::SystemPrompt>
: atlas::atlas_detail::hash_base<wjh::chat::SystemPrompt>
{};
#endif

namespace wjh {
namespace chat {

/**
 * @brief Strong type wrapper for bool
 *
 * Generated by Atlas Strong Type Generator.
 * Generation parameters:
 * - kind: class
 * - type_namespace: wjh::chat
 * - type_name: ShowHelp
 * - description: bool; ==, bool
 * - default_value: "false"
 */
class ShowHelp
: private atlas::strong_type_tag<ShowHelp>
{
    bool value = static_cast<bool>(false);

public:
    using atlas_value_type = bool;

    constexpr explicit ShowHelp() = default;

    template <
        typename... ArgTs,
        typename std::enable_if<
            std::is_constructible<bool, ArgTs...>::value,
            bool>::type = true>
    constexpr explicit ShowHelp(ArgTs && ... args)
    : value(std::forward<ArgTs>(args)...)
    { }

    /**
     * Access to immediate underlying value via ADL.
     */
    friend constexpr bool const & atlas_value_for(ShowHelp const & self) noexcept {
        return self.value;
    }
    friend constexpr bool & atlas_value_for(ShowHelp & self) noexcept {
        return self.value;
    }
    friend constexpr auto atlas_value_for(ShowHelp && self) noexcept
        -> typename std::enable_if<
            std::is_move_constructible<bool>::value,
            bool>::type
    {
        return std::move(self.value);
    }
    friend constexpr auto atlas_value_for(
        ShowHelp && self,
        atlas::atlas_detail::ref_tag) noexcept
        -> bool &&
    {
        return static_cast<bool &&>(self.value);
    }

    /**
     * Lifetime guard: converting an expiring object to a type that borrows
     * its storage would dangle.  Deleted, non-explicit, and rvalue-only, so
     * every conversion route from a temporary -- static_cast,
     * copy-initialization, and implicit conversion -- is rejected at compile
     * time.  Inert (never a candidate) unless the target borrows and the
     * underlying type owns; specialize atlas::borrow_policy to adjust.
     * The remedy at a rejected call site is to bind the source to a named
     * variable first.
     */
    template <
        typename BorrowTargetT,
        typename = atlas::atlas_detail::enable_if_t<
            atlas::borrow_policy<BorrowTargetT, bool>::value>>
    operator BorrowTargetT () const &&
#if defined(__cpp_deleted_function) && __cpp_deleted_function >= 202403L \
    && __cplusplus > 202302L
    = delete("converting an expiring object to a type that borrows its "
        "storage dangles; bind the source to a named variable first");
#else
    = delete;
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

    /**
     * Return the result of casting the wrapped object to bool.
     */
    constexpr explicit operator bool () const
    noexcept(noexcept(static_cast<bool>(
        std::declval<bool const&>())))
    {
        return static_cast<bool>(value);
    }
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

    /**
     * Is @p lhs.value == @p rhs.value?
     */
    friend constexpr bool operator == (
        ShowHelp const & lhs,
        ShowHelp const & rhs)
    noexcept(noexcept(std::declval<bool const&>() == std::declval<bool const&>()))
    {
        return lhs.value == rhs.value;
    }
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
};
} // namespace chat
} // namespace wjh


#if __cplusplus < 202002L
template <>
struct std::hash<wjh::chat::ShowHelp>
: atlas::atlas_detail::hash_base<wjh::chat::ShowHelp>
{};
#endif

namespace wjh {
namespace chat {

/**
 * @brief Strong type wrapper for bool
 *
 * Generated by Atlas Strong Type Generator.
 * Generation parameters:
 * - kind: class
 * - type_namespace: wjh::chat
 * - type_name: ShowConfig
 * - description: bool; ==, bool
 * - default_value: "false"
 */
class ShowConfig
: private atlas::strong_type_tag<ShowConfig>
{
    bool value = static_cast<bool>(false);

public:
    using atlas_value_type = bool;

    constexpr explicit ShowConfig() = default;

    template <
        typename... ArgTs,
        typename std::enable_if<
            std::is_constructible<bool, ArgTs...>::value,
            bool>::type = true>
    constexpr explicit ShowConfig(ArgTs && ... args)
    : value(std::forward<ArgTs>(args)...)
    { }

    /**
     * Access to immediate underlying value via ADL.
     */
    friend constexpr bool const & atlas_value_for(ShowConfig const & self) noexcept {
        return self.value;
    }
    friend constexpr bool & atlas_value_for(ShowConfig & self) noexcept {
        return self.value;
    }
    friend constexpr auto atlas_value_for(ShowConfig && self) noexcept
        -> typename std::enable_if<
            std::is_move_constructible<bool>::value,
            bool>::type
    {
        return std::move(self.value);
    }
    friend constexpr auto atlas_value_for(
        ShowConfig && self,
        atlas::atlas_detail::ref_tag) noexcept
        -> bool &&
    {
        return static_cast<bool &&>(self.value);
    }

    /**
     * Lifetime guard: converting an expiring object to a type that borrows
     * its storage would dangle.  Deleted, non-explicit, and rvalue-only, so
     * every conversion route from a temporary -- static_cast,
     * copy-initialization, and implicit conversion -- is rejected at compile
     * time.  Inert (never a candidate) unless the target borrows and the
     * underlying type owns; specialize atlas::borrow_policy to adjust.
     * The remedy at a rejected call site is to bind the source to a named
     * variable first.
     */
    template <
        typename BorrowTargetT,
        typename = atlas::atlas_detail::enable_if_t<
            atlas::borrow_policy<BorrowTargetT, bool>::value>>
    operator BorrowTargetT () const &&
#if defined(__cpp_deleted_function) && __cpp_deleted_function >= 202403L \
    && __cplusplus > 202302L
    = delete("converting an expiring object to a type that borrows its "
        "storage dangles; bind the source to a named variable first");
#else
    = delete;
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

    /**
     * Return the result of casting the wrapped object to bool.
     */
    constexpr explicit operator bool () const
    noexcept(noexcept(static_cast<bool>(
        std::declval<bool const&>())))
    {
        return static_cast<bool>(value);
    }
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

    /**
     * Is @p lhs.value == @p rhs.value?
     */
    friend constexpr bool operator == (
        ShowConfig const & lhs,
        ShowConfig const & rhs)
    noexcept(noexcept(std::declval<bool const&>() == std::declval<bool const&>()))
    {
        return lhs.value == rhs.value;
    }
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
};
} // namespace chat
} // namespace wjh


#if __cplusplus < 202002L
template <>
struct std::hash<wjh::chat::ShowConfig>
: atlas::atlas_detail::hash_base<wjh::chat::ShowConfig>
{};
#endif

namespace wjh {
namespace chat {

/**
 * @brief Strong type wrapper for bool
 *
 * Generated by Atlas Strong Type Generator.
 * Generation parameters:
 * - kind: class
 * - type_namespace: wjh::chat
 * - type_name: YoloMode
 * - description: bool; ==, bool
 * - default_value: "false"
 */
class YoloMode
: private atlas::strong_type_tag<YoloMode>
{
    bool value = static_cast<bool>(false);

public:
    using atlas_value_type = bool;

    constexpr explicit YoloMode() = default;

    template <
        typename... ArgTs,
        typename std::enable_if<
            std::is_constructible<bool, ArgTs...>::value,
            bool>::type = true>
    constexpr explicit YoloMode(ArgTs && ... args)
    : value(std::forward<ArgTs>(args)...)
    { }

    /**
     * Access to immediate underlying value via ADL.
     */
    friend constexpr bool const & atlas_value_for(YoloMode const & self) noexcept {
        return self.value;
    }
    friend constexpr bool & atlas_value_for(YoloMode & self) noexcept {
        return self.value;
    }
    friend constexpr auto atlas_value_for(YoloMode && self) noexcept
        -> typename std::enable_if<
            std::is_move_constructible<bool>::value,
            bool>::type
    {
        return std::move(self.value);
    }
    friend constexpr auto atlas_value_for(
        YoloMode && self,
        atlas::atlas_detail::ref_tag) noexcept
        -> bool &&
    {
        return static_cast<bool &&>(self.value);
    }

    /**
     * Lifetime guard: converting an expiring object to a type that borrows
     * its storage would dangle.  Deleted, non-explicit, and rvalue-only, so
     * every conversion route from a temporary -- static_cast,
     * copy-initialization, and implicit conversion -- is rejected at compile
     * time.  Inert (never a candidate) unless the target borrows and the
     * underlying type owns; specialize atlas::borrow_policy to adjust.
     * The remedy at a rejected call site is to bind the source to a named
     * variable first.
     */
    template <
        typename BorrowTargetT,
        typename = atlas::atlas_detail::enable_if_t<
            atlas::borrow_policy<BorrowTargetT, bool>::value>>
    operator BorrowTargetT () const &&
#if defined(__cpp_deleted_function) && __cpp_deleted_function >= 202403L \
    && __cplusplus > 202302L
    = delete("converting an expiring object to a type that borrows its "
        "storage dangles; bind the source to a named variable first");
#else
    = delete;
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

    /**
     * Return the result of casting the wrapped object to bool.
     */
    constexpr explicit operator bool () const
    noexcept(noexcept(static_cast<bool>(
        std::declval<bool const&>())))
    {
        return static_cast<bool>(value);
    }
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

    /**
     * Is @p lhs.value == @p rhs.value?
     */
    friend constexpr bool operator == (
        YoloMode const & lhs,
        YoloMode const & rhs)
    noexcept(noexcept(std::declval<bool const&>() == std::declval<bool const&>()))
    {
        return lhs.value == rhs.value;
    }
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
};
} // namespace chat
} // namespace wjh


#if __cplusplus < 202002L
template <>
struct std::hash<wjh::chat::YoloMode>
: atlas::atlas_detail::hash_base<wjh::chat::YoloMode>
{};
#endif

namespace wjh {
namespace chat {

/**
 * @brief Strong type wrapper for std::string
 *
 * Generated by Atlas Strong Type Generator.
 * Generation parameters:
 * - kind: class
 * - type_namespace: wjh::chat
 * - type_name: ProgramName
 * - description: std::string; <=>
 * - default_value: ""
 */
class ProgramName
: private atlas::strong_type_tag<ProgramName>
{
    std::string value;

public:
    using atlas_value_type = std::string;

    constexpr explicit ProgramName() = default;

    template <
        typename... ArgTs,
        typename std::enable_if<
            std::is_constructible<std::string, ArgTs...>::value,
            bool>::type = true>
    constexpr explicit ProgramName(ArgTs && ... args)
    : value(std::forward<ArgTs>(args)...)
    { }

    /**
     * Access to immediate underlying value via ADL.
     */
    friend constexpr std::string const & atlas_value_for(ProgramName const & self) noexcept {
        return self.value;
    }
    friend constexpr std::string & atlas_value_for(ProgramName & self) noexcept {
        return self.value;
    }
    friend constexpr auto atlas_value_for(ProgramName && self) noexcept
        -> typename std::enable_if<
            std::is_move_constructible<std::string>::value,
            std::string>::type
    {
        return std::move(self.value);
    }
    friend constexpr auto atlas_value_for(
        ProgramName && self,
        atlas::atlas_detail::ref_tag) noexcept
        -> std::string &&
    {
        return static_cast<std::string &&>(self.value);
    }

    /**
     * Lifetime guard: converting an expiring object to a type that borrows
     * its storage would dangle.  Deleted, non-explicit, and rvalue-only, so
     * every conversion route from a temporary -- static_cast,
     * copy-initialization, and implicit conversion -- is rejected at compile
     * time.  Inert (never a candidate) unless the target borrows and the
     * underlying type owns; specialize atlas::borrow_policy to adjust.
     * The remedy at a rejected call site is to bind the source to a named
     * variable first.
     */
    template <
        typename BorrowTargetT,
        typename = atlas::atlas_detail::enable_if_t<
            atlas::borrow_policy<BorrowTargetT, std::string>::value>>
    operator BorrowTargetT () const &&
#if defined(__cpp_deleted_function) && __cpp_deleted_function >= 202403L \
    && __cplusplus > 202302L
    = delete("converting an expiring object to a type that borrows its "
        "storage dangles; bind the source to a named variable first");
#else
    = delete;
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default three-way comparison (spaceship) operator.
     */
    friend constexpr auto operator <=> (
        ProgramName const &,
        ProgramName const &) = default;
#else
    /**
     * Comparison operators (C++17 fallback for spaceship operator).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator < (
        ProgramName const & lhs,
        ProgramName const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() <
        std::declval<std::string const &>()))
    {
        return lhs.value < rhs.value;
    }

    friend constexpr bool operator <= (
        ProgramName const & lhs,
        ProgramName const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() <=
        std::declval<std::string const &>()))
    {
        return lhs.value <= rhs.value;
    }

    friend constexpr bool operator > (
        ProgramName const & lhs,
        ProgramName const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() >
        std::declval<std::string const &>()))
    {
        return lhs.value > rhs.value;
    }

    friend constexpr bool operator >= (
        ProgramName const & lhs,
        ProgramName const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() >=
        std::declval<std::string const &>()))
    {
        return lhs.value >= rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default equality comparison operator.
     * Provided with spaceship operator for optimal performance.
     */
    friend constexpr bool operator == (
        ProgramName const &,
        ProgramName const &) = default;
#else
    /**
     * Equality comparison operators (C++17 fallback).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator == (
        ProgramName const & lhs,
        ProgramName const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() ==
        std::declval<std::string const &>()))
    {
        return lhs.value == rhs.value;
    }

    friend constexpr bool operator != (
        ProgramName const & lhs,
        ProgramName const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() !=
        std::declval<std::string const &>()))
    {
        return lhs.value != rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
};
} // namespace chat
} // namespace wjh


#if __cplusplus < 202002L
template <>
struct std::hash<wjh::chat::ProgramName>
: atlas::atlas_detail::hash_base<wjh::chat::ProgramName>
{};
#endif

namespace wjh {
namespace chat {

/**
 * @brief Strong type wrapper for std::string
 *
 * Generated by Atlas Strong Type Generator.
 * Generation parameters:
 * - kind: class
 * - type_namespace: wjh::chat
 * - type_name: HelpText
 * - description: std::string; <=>
 * - default_value: ""
 */
class HelpText
: private atlas::strong_type_tag<HelpText>
{
    std::string value;

public:
    using atlas_value_type = std::string;

    constexpr explicit HelpText() = default;

    template <
        typename... ArgTs,
        typename std::enable_if<
            std::is_constructible<std::string, ArgTs...>::value,
            bool>::type = true>
    constexpr explicit HelpText(ArgTs && ... args)
    : value(std::forward<ArgTs>(args)...)
    { }

    /**
     * Access to immediate underlying value via ADL.
     */
    friend constexpr std::string const & atlas_value_for(HelpText const & self) noexcept {
        return self.value;
    }
    friend constexpr std::string & atlas_value_for(HelpText & self) noexcept {
        return self.value;
    }
    friend constexpr auto atlas_value_for(HelpText && self) noexcept
        -> typename std::enable_if<
            std::is_move_constructible<std::string>::value,
            std::string>::type
    {
        return std::move(self.value);
    }
    friend constexpr auto atlas_value_for(
        HelpText && self,
        atlas::atlas_detail::ref_tag) noexcept
        -> std::string &&
    {
        return static_cast<std::string &&>(self.value);
    }

    /**
     * Lifetime guard: converting an expiring object to a type that borrows
     * its storage would dangle.  Deleted, non-explicit, and rvalue-only, so
     * every conversion route from a temporary -- static_cast,
     * copy-initialization, and implicit conversion -- is rejected at compile
     * time.  Inert (never a candidate) unless the target borrows and the
     * underlying type owns; specialize atlas::borrow_policy to adjust.
     * The remedy at a rejected call site is to bind the source to a named
     * variable first.
     */
    template <
        typename BorrowTargetT,
        typename = atlas::atlas_detail::enable_if_t<
            atlas::borrow_policy<BorrowTargetT, std::string>::value>>
    operator BorrowTargetT () const &&
#if defined(__cpp_deleted_function) && __cpp_deleted_function >= 202403L \
    && __cplusplus > 202302L
    = delete("converting an expiring object to a type that borrows its "
        "storage dangles; bind the source to a named variable first");
#else
    = delete;
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default three-way comparison (spaceship) operator.
     */
    friend constexpr auto operator <=> (
        HelpText const &,
        HelpText const &) = default;
#else
    /**
     * Comparison operators (C++17 fallback for spaceship operator).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator < (
        HelpText const & lhs,
        HelpText const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() <
        std::declval<std::string const &>()))
    {
        return lhs.value < rhs.value;
    }

    friend constexpr bool operator <= (
        HelpText const & lhs,
        HelpText const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() <=
        std::declval<std::string const &>()))
    {
        return lhs.value <= rhs.value;
    }

    friend constexpr bool operator > (
        HelpText const & lhs,
        HelpText const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() >
        std::declval<std::string const &>()))
    {
        return lhs.value > rhs.value;
    }

    friend constexpr bool operator >= (
        HelpText const & lhs,
        HelpText const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() >=
        std::declval<std::string const &>()))
    {
        return lhs.value >= rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default equality comparison operator.
     * Provided with spaceship operator for optimal performance.
     */
    friend constexpr bool operator == (
        HelpText const &,
        HelpText const &) = default;
#else
    /**
     * Equality comparison operators (C++17 fallback).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator == (
        HelpText const & lhs,
        HelpText const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() ==
        std::declval<std::string const &>()))
    {
        return lhs.value == rhs.value;
    }

    friend constexpr bool operator != (
        HelpText const & lhs,
        HelpText const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() !=
        std::declval<std::string const &>()))
    {
        return lhs.value != rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
};
} // namespace chat
} // namespace wjh


#if __cplusplus < 202002L
template <>
struct std::hash<wjh::chat::HelpText>
: atlas::atlas_detail::hash_base<wjh::chat::HelpText>
{};
#endif

namespace wjh {
namespace chat {

/**
 * @brief Strong type wrapper for std::string
 *
 * Generated by Atlas Strong Type Generator.
 * Generation parameters:
 * - kind: class
 * - type_namespace: wjh::chat
 * - type_name: UserInput
 * - description: std::string; <=>
 * - default_value: ""
 */
class UserInput
: private atlas::strong_type_tag<UserInput>
{
    std::string value;

public:
    using atlas_value_type = std::string;

    constexpr explicit UserInput() = default;

    template <
        typename... ArgTs,
        typename std::enable_if<
            std::is_constructible<std::string, ArgTs...>::value,
            bool>::type = true>
    constexpr explicit UserInput(ArgTs && ... args)
    : value(std::forward<ArgTs>(args)...)
    { }

    /**
     * Access to immediate underlying value via ADL.
     */
    friend constexpr std::string const & atlas_value_for(UserInput const & self) noexcept {
        return self.value;
    }
    friend constexpr std::string & atlas_value_for(UserInput & self) noexcept {
        return self.value;
    }
    friend constexpr auto atlas_value_for(UserInput && self) noexcept
        -> typename std::enable_if<
            std::is_move_constructible<std::string>::value,
            std::string>::type
    {
        return std::move(self.value);
    }
    friend constexpr auto atlas_value_for(
        UserInput && self,
        atlas::atlas_detail::ref_tag) noexcept
        -> std::string &&
    {
        return static_cast<std::string &&>(self.value);
    }

    /**
     * Lifetime guard: converting an expiring object to a type that borrows
     * its storage would dangle.  Deleted, non-explicit, and rvalue-only, so
     * every conversion route from a temporary -- static_cast,
     * copy-initialization, and implicit conversion -- is rejected at compile
     * time.  Inert (never a candidate) unless the target borrows and the
     * underlying type owns; specialize atlas::borrow_policy to adjust.
     * The remedy at a rejected call site is to bind the source to a named
     * variable first.
     */
    template <
        typename BorrowTargetT,
        typename = atlas::atlas_detail::enable_if_t<
            atlas::borrow_policy<BorrowTargetT, std::string>::value>>
    operator BorrowTargetT () const &&
#if defined(__cpp_deleted_function) && __cpp_deleted_function >= 202403L \
    && __cplusplus > 202302L
    = delete("converting an expiring object to a type that borrows its "
        "storage dangles; bind the source to a named variable first");
#else
    = delete;
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default three-way comparison (spaceship) operator.
     */
    friend constexpr auto operator <=> (
        UserInput const &,
        UserInput const &) = default;
#else
    /**
     * Comparison operators (C++17 fallback for spaceship operator).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator < (
        UserInput const & lhs,
        UserInput const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() <
        std::declval<std::string const &>()))
    {
        return lhs.value < rhs.value;
    }

    friend constexpr bool operator <= (
        UserInput const & lhs,
        UserInput const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() <=
        std::declval<std::string const &>()))
    {
        return lhs.value <= rhs.value;
    }

    friend constexpr bool operator > (
        UserInput const & lhs,
        UserInput const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() >
        std::declval<std::string const &>()))
    {
        return lhs.value > rhs.value;
    }

    friend constexpr bool operator >= (
        UserInput const & lhs,
        UserInput const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() >=
        std::declval<std::string const &>()))
    {
        return lhs.value >= rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default equality comparison operator.
     * Provided with spaceship operator for optimal performance.
     */
    friend constexpr bool operator == (
        UserInput const &,
        UserInput const &) = default;
#else
    /**
     * Equality comparison operators (C++17 fallback).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator == (
        UserInput const & lhs,
        UserInput const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() ==
        std::declval<std::string const &>()))
    {
        return lhs.value == rhs.value;
    }

    friend constexpr bool operator != (
        UserInput const & lhs,
        UserInput const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() !=
        std::declval<std::string const &>()))
    {
        return lhs.value != rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
};
} // namespace chat
} // namespace wjh


#if __cplusplus < 202002L
template <>
struct std::hash<wjh::chat::UserInput>
: atlas::atlas_detail::hash_base<wjh::chat::UserInput>
{};
#endif

namespace wjh {
namespace chat {

/**
 * @brief Strong type wrapper for std::string
 *
 * Generated by Atlas Strong Type Generator.
 * Generation parameters:
 * - kind: class
 * - type_namespace: wjh::chat
 * - type_name: AssistantResponse
 * - description: std::string; <=>
 * - default_value: ""
 */
class AssistantResponse
: private atlas::strong_type_tag<AssistantResponse>
{
    std::string value;

public:
    using atlas_value_type = std::string;

    constexpr explicit AssistantResponse() = default;

    template <
        typename... ArgTs,
        typename std::enable_if<
            std::is_constructible<std::string, ArgTs...>::value,
            bool>::type = true>
    constexpr explicit AssistantResponse(ArgTs && ... args)
    : value(std::forward<ArgTs>(args)...)
    { }

    /**
     * Access to immediate underlying value via ADL.
     */
    friend constexpr std::string const & atlas_value_for(AssistantResponse const & self) noexcept {
        return self.value;
    }
    friend constexpr std::string & atlas_value_for(AssistantResponse & self) noexcept {
        return self.value;
    }
    friend constexpr auto atlas_value_for(AssistantResponse && self) noexcept
        -> typename std::enable_if<
            std::is_move_constructible<std::string>::value,
            std::string>::type
    {
        return std::move(self.value);
    }
    friend constexpr auto atlas_value_for(
        AssistantResponse && self,
        atlas::atlas_detail::ref_tag) noexcept
        -> std::string &&
    {
        return static_cast<std::string &&>(self.value);
    }

    /**
     * Lifetime guard: converting an expiring object to a type that borrows
     * its storage would dangle.  Deleted, non-explicit, and rvalue-only, so
     * every conversion route from a temporary -- static_cast,
     * copy-initialization, and implicit conversion -- is rejected at compile
     * time.  Inert (never a candidate) unless the target borrows and the
     * underlying type owns; specialize atlas::borrow_policy to adjust.
     * The remedy at a rejected call site is to bind the source to a named
     * variable first.
     */
    template <
        typename BorrowTargetT,
        typename = atlas::atlas_detail::enable_if_t<
            atlas::borrow_policy<BorrowTargetT, std::string>::value>>
    operator BorrowTargetT () const &&
#if defined(__cpp_deleted_function) && __cpp_deleted_function >= 202403L \
    && __cplusplus > 202302L
    = delete("converting an expiring object to a type that borrows its "
        "storage dangles; bind the source to a named variable first");
#else
    = delete;
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default three-way comparison (spaceship) operator.
     */
    friend constexpr auto operator <=> (
        AssistantResponse const &,
        AssistantResponse const &) = default;
#else
    /**
     * Comparison operators (C++17 fallback for spaceship operator).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator < (
        AssistantResponse const & lhs,
        AssistantResponse const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() <
        std::declval<std::string const &>()))
    {
        return lhs.value < rhs.value;
    }

    friend constexpr bool operator <= (
        AssistantResponse const & lhs,
        AssistantResponse const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() <=
        std::declval<std::string const &>()))
    {
        return lhs.value <= rhs.value;
    }

    friend constexpr bool operator > (
        AssistantResponse const & lhs,
        AssistantResponse const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() >
        std::declval<std::string const &>()))
    {
        return lhs.value > rhs.value;
    }

    friend constexpr bool operator >= (
        AssistantResponse const & lhs,
        AssistantResponse const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() >=
        std::declval<std::string const &>()))
    {
        return lhs.value >= rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default equality comparison operator.
     * Provided with spaceship operator for optimal performance.
     */
    friend constexpr bool operator == (
        AssistantResponse const &,
        AssistantResponse const &) = default;
#else
    /**
     * Equality comparison operators (C++17 fallback).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator == (
        AssistantResponse const & lhs,
        AssistantResponse const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() ==
        std::declval<std::string const &>()))
    {
        return lhs.value == rhs.value;
    }

    friend constexpr bool operator != (
        AssistantResponse const & lhs,
        AssistantResponse const & rhs)
    noexcept(noexcept(std::declval<std::string const &>() !=
        std::declval<std::string const &>()))
    {
        return lhs.value != rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
};
} // namespace chat
} // namespace wjh


#if __cplusplus < 202002L
template <>
struct std::hash<wjh::chat::AssistantResponse>
: atlas::atlas_detail::hash_base<wjh::chat::AssistantResponse>
{};
#endif

namespace wjh {
namespace chat {

/**
 * @brief Strong type wrapper for std::uint32_t
 *
 * Generated by Atlas Strong Type Generator.
 * Generation parameters:
 * - kind: class
 * - type_namespace: wjh::chat
 * - type_name: PromptTokens
 * - description: std::uint32_t; +, <=>
 * - default_value: "0u"
 */
class PromptTokens
: private atlas::strong_type_tag<PromptTokens>
{
    std::uint32_t value = static_cast<std::uint32_t>(0u);

public:
    using atlas_value_type = std::uint32_t;

    constexpr explicit PromptTokens() = default;

    template <
        typename... ArgTs,
        typename std::enable_if<
            std::is_constructible<std::uint32_t, ArgTs...>::value,
            bool>::type = true>
    constexpr explicit PromptTokens(ArgTs && ... args)
    : value(std::forward<ArgTs>(args)...)
    { }

    /**
     * Access to immediate underlying value via ADL.
     */
    friend constexpr std::uint32_t const & atlas_value_for(PromptTokens const & self) noexcept {
        return self.value;
    }
    friend constexpr std::uint32_t & atlas_value_for(PromptTokens & self) noexcept {
        return self.value;
    }
    friend constexpr auto atlas_value_for(PromptTokens && self) noexcept
        -> typename std::enable_if<
            std::is_move_constructible<std::uint32_t>::value,
            std::uint32_t>::type
    {
        return std::move(self.value);
    }
    friend constexpr auto atlas_value_for(
        PromptTokens && self,
        atlas::atlas_detail::ref_tag) noexcept
        -> std::uint32_t &&
    {
        return static_cast<std::uint32_t &&>(self.value);
    }

    /**
     * Lifetime guard: converting an expiring object to a type that borrows
     * its storage would dangle.  Deleted, non-explicit, and rvalue-only, so
     * every conversion route from a temporary -- static_cast,
     * copy-initialization, and implicit conversion -- is rejected at compile
     * time.  Inert (never a candidate) unless the target borrows and the
     * underlying type owns; specialize atlas::borrow_policy to adjust.
     * The remedy at a rejected call site is to bind the source to a named
     * variable first.
     */
    template <
        typename BorrowTargetT,
        typename = atlas::atlas_detail::enable_if_t<
            atlas::borrow_policy<BorrowTargetT, std::uint32_t>::value>>
    operator BorrowTargetT () const &&
#if defined(__cpp_deleted_function) && __cpp_deleted_function >= 202403L \
    && __cplusplus > 202302L
    = delete("converting an expiring object to a type that borrows its "
        "storage dangles; bind the source to a named variable first");
#else
    = delete;
#endif

    /**
     * Apply + assignment to the wrapped objects.
     */
    friend constexpr PromptTokens & operator += (
        PromptTokens & lhs,
        PromptTokens const & rhs)
#if defined(__clang__)
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wunevaluated-expression"
#endif
    noexcept(noexcept(std::declval<std::uint32_t &>() += std::declval<std::uint32_t const &>()))
#if defined(__clang__)
#pragma clang diagnostic pop
#endif
    {
        lhs.value += rhs.value;
        return lhs;
    }
    /**
     * Apply the binary operator + to the wrapped object.
     */
    friend constexpr PromptTokens operator + (
        PromptTokens lhs,
        PromptTokens const & rhs)
    noexcept(noexcept(lhs += rhs))
    {
        lhs += rhs;
        return lhs;
    }

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default three-way comparison (spaceship) operator.
     */
    friend constexpr auto operator <=> (
        PromptTokens const &,
        PromptTokens const &) = default;
#else
    /**
     * Comparison operators (C++17 fallback for spaceship operator).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator < (
        PromptTokens const & lhs,
        PromptTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() <
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value < rhs.value;
    }

    friend constexpr bool operator <= (
        PromptTokens const & lhs,
        PromptTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() <=
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value <= rhs.value;
    }

    friend constexpr bool operator > (
        PromptTokens const & lhs,
        PromptTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() >
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value > rhs.value;
    }

    friend constexpr bool operator >= (
        PromptTokens const & lhs,
        PromptTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() >=
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value >= rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default equality comparison operator.
     * Provided with spaceship operator for optimal performance.
     */
    friend constexpr bool operator == (
        PromptTokens const &,
        PromptTokens const &) = default;
#else
    /**
     * Equality comparison operators (C++17 fallback).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator == (
        PromptTokens const & lhs,
        PromptTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() ==
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value == rhs.value;
    }

    friend constexpr bool operator != (
        PromptTokens const & lhs,
        PromptTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() !=
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value != rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
};
} // namespace chat
} // namespace wjh


#if __cplusplus < 202002L
template <>
struct std::hash<wjh::chat::PromptTokens>
: atlas::atlas_detail::hash_base<wjh::chat::PromptTokens>
{};
#endif

namespace wjh {
namespace chat {

/**
 * @brief Strong type wrapper for std::uint32_t
 *
 * Generated by Atlas Strong Type Generator.
 * Generation parameters:
 * - kind: class
 * - type_namespace: wjh::chat
 * - type_name: CompletionTokens
 * - description: std::uint32_t; +, <=>
 * - default_value: "0u"
 */
class CompletionTokens
: private atlas::strong_type_tag<CompletionTokens>
{
    std::uint32_t value = static_cast<std::uint32_t>(0u);

public:
    using atlas_value_type = std::uint32_t;

    constexpr explicit CompletionTokens() = default;

    template <
        typename... ArgTs,
        typename std::enable_if<
            std::is_constructible<std::uint32_t, ArgTs...>::value,
            bool>::type = true>
    constexpr explicit CompletionTokens(ArgTs && ... args)
    : value(std::forward<ArgTs>(args)...)
    { }

    /**
     * Access to immediate underlying value via ADL.
     */
    friend constexpr std::uint32_t const & atlas_value_for(CompletionTokens const & self) noexcept {
        return self.value;
    }
    friend constexpr std::uint32_t & atlas_value_for(CompletionTokens & self) noexcept {
        return self.value;
    }
    friend constexpr auto atlas_value_for(CompletionTokens && self) noexcept
        -> typename std::enable_if<
            std::is_move_constructible<std::uint32_t>::value,
            std::uint32_t>::type
    {
        return std::move(self.value);
    }
    friend constexpr auto atlas_value_for(
        CompletionTokens && self,
        atlas::atlas_detail::ref_tag) noexcept
        -> std::uint32_t &&
    {
        return static_cast<std::uint32_t &&>(self.value);
    }

    /**
     * Lifetime guard: converting an expiring object to a type that borrows
     * its storage would dangle.  Deleted, non-explicit, and rvalue-only, so
     * every conversion route from a temporary -- static_cast,
     * copy-initialization, and implicit conversion -- is rejected at compile
     * time.  Inert (never a candidate) unless the target borrows and the
     * underlying type owns; specialize atlas::borrow_policy to adjust.
     * The remedy at a rejected call site is to bind the source to a named
     * variable first.
     */
    template <
        typename BorrowTargetT,
        typename = atlas::atlas_detail::enable_if_t<
            atlas::borrow_policy<BorrowTargetT, std::uint32_t>::value>>
    operator BorrowTargetT () const &&
#if defined(__cpp_deleted_function) && __cpp_deleted_function >= 202403L \
    && __cplusplus > 202302L
    = delete("converting an expiring object to a type that borrows its "
        "storage dangles; bind the source to a named variable first");
#else
    = delete;
#endif

    /**
     * Apply + assignment to the wrapped objects.
     */
    friend constexpr CompletionTokens & operator += (
        CompletionTokens & lhs,
        CompletionTokens const & rhs)
#if defined(__clang__)
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wunevaluated-expression"
#endif
    noexcept(noexcept(std::declval<std::uint32_t &>() += std::declval<std::uint32_t const &>()))
#if defined(__clang__)
#pragma clang diagnostic pop
#endif
    {
        lhs.value += rhs.value;
        return lhs;
    }
    /**
     * Apply the binary operator + to the wrapped object.
     */
    friend constexpr CompletionTokens operator + (
        CompletionTokens lhs,
        CompletionTokens const & rhs)
    noexcept(noexcept(lhs += rhs))
    {
        lhs += rhs;
        return lhs;
    }

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default three-way comparison (spaceship) operator.
     */
    friend constexpr auto operator <=> (
        CompletionTokens const &,
        CompletionTokens const &) = default;
#else
    /**
     * Comparison operators (C++17 fallback for spaceship operator).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator < (
        CompletionTokens const & lhs,
        CompletionTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() <
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value < rhs.value;
    }

    friend constexpr bool operator <= (
        CompletionTokens const & lhs,
        CompletionTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() <=
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value <= rhs.value;
    }

    friend constexpr bool operator > (
        CompletionTokens const & lhs,
        CompletionTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() >
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value > rhs.value;
    }

    friend constexpr bool operator >= (
        CompletionTokens const & lhs,
        CompletionTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() >=
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value >= rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default equality comparison operator.
     * Provided with spaceship operator for optimal performance.
     */
    friend constexpr bool operator == (
        CompletionTokens const &,
        CompletionTokens const &) = default;
#else
    /**
     * Equality comparison operators (C++17 fallback).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator == (
        CompletionTokens const & lhs,
        CompletionTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() ==
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value == rhs.value;
    }

    friend constexpr bool operator != (
        CompletionTokens const & lhs,
        CompletionTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() !=
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value != rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
};
} // namespace chat
} // namespace wjh


#if __cplusplus < 202002L
template <>
struct std::hash<wjh::chat::CompletionTokens>
: atlas::atlas_detail::hash_base<wjh::chat::CompletionTokens>
{};
#endif

namespace wjh {
namespace chat {

/**
 * @brief Strong type wrapper for std::uint32_t
 *
 * Generated by Atlas Strong Type Generator.
 * Generation parameters:
 * - kind: class
 * - type_namespace: wjh::chat
 * - type_name: TotalTokens
 * - description: std::uint32_t; +, <=>
 * - default_value: "0u"
 */
class TotalTokens
: private atlas::strong_type_tag<TotalTokens>
{
    std::uint32_t value = static_cast<std::uint32_t>(0u);

public:
    using atlas_value_type = std::uint32_t;

    constexpr explicit TotalTokens() = default;

    template <
        typename... ArgTs,
        typename std::enable_if<
            std::is_constructible<std::uint32_t, ArgTs...>::value,
            bool>::type = true>
    constexpr explicit TotalTokens(ArgTs && ... args)
    : value(std::forward<ArgTs>(args)...)
    { }

    /**
     * Access to immediate underlying value via ADL.
     */
    friend constexpr std::uint32_t const & atlas_value_for(TotalTokens const & self) noexcept {
        return self.value;
    }
    friend constexpr std::uint32_t & atlas_value_for(TotalTokens & self) noexcept {
        return self.value;
    }
    friend constexpr auto atlas_value_for(TotalTokens && self) noexcept
        -> typename std::enable_if<
            std::is_move_constructible<std::uint32_t>::value,
            std::uint32_t>::type
    {
        return std::move(self.value);
    }
    friend constexpr auto atlas_value_for(
        TotalTokens && self,
        atlas::atlas_detail::ref_tag) noexcept
        -> std::uint32_t &&
    {
        return static_cast<std::uint32_t &&>(self.value);
    }

    /**
     * Lifetime guard: converting an expiring object to a type that borrows
     * its storage would dangle.  Deleted, non-explicit, and rvalue-only, so
     * every conversion route from a temporary -- static_cast,
     * copy-initialization, and implicit conversion -- is rejected at compile
     * time.  Inert (never a candidate) unless the target borrows and the
     * underlying type owns; specialize atlas::borrow_policy to adjust.
     * The remedy at a rejected call site is to bind the source to a named
     * variable first.
     */
    template <
        typename BorrowTargetT,
        typename = atlas::atlas_detail::enable_if_t<
            atlas::borrow_policy<BorrowTargetT, std::uint32_t>::value>>
    operator BorrowTargetT () const &&
#if defined(__cpp_deleted_function) && __cpp_deleted_function >= 202403L \
    && __cplusplus > 202302L
    = delete("converting an expiring object to a type that borrows its "
        "storage dangles; bind the source to a named variable first");
#else
    = delete;
#endif

    /**
     * Apply + assignment to the wrapped objects.
     */
    friend constexpr TotalTokens & operator += (
        TotalTokens & lhs,
        TotalTokens const & rhs)
#if defined(__clang__)
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wunevaluated-expression"
#endif
    noexcept(noexcept(std::declval<std::uint32_t &>() += std::declval<std::uint32_t const &>()))
#if defined(__clang__)
#pragma clang diagnostic pop
#endif
    {
        lhs.value += rhs.value;
        return lhs;
    }
    /**
     * Apply the binary operator + to the wrapped object.
     */
    friend constexpr TotalTokens operator + (
        TotalTokens lhs,
        TotalTokens const & rhs)
    noexcept(noexcept(lhs += rhs))
    {
        lhs += rhs;
        return lhs;
    }

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default three-way comparison (spaceship) operator.
     */
    friend constexpr auto operator <=> (
        TotalTokens const &,
        TotalTokens const &) = default;
#else
    /**
     * Comparison operators (C++17 fallback for spaceship operator).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator < (
        TotalTokens const & lhs,
        TotalTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() <
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value < rhs.value;
    }

    friend constexpr bool operator <= (
        TotalTokens const & lhs,
        TotalTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() <=
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value <= rhs.value;
    }

    friend constexpr bool operator > (
        TotalTokens const & lhs,
        TotalTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() >
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value > rhs.value;
    }

    friend constexpr bool operator >= (
        TotalTokens const & lhs,
        TotalTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() >=
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value >= rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wfloat-equal"
#endif

#if defined(__cpp_impl_three_way_comparison) && \
    __cpp_impl_three_way_comparison >= 201907L
    /**
     * The default equality comparison operator.
     * Provided with spaceship operator for optimal performance.
     */
    friend constexpr bool operator == (
        TotalTokens const &,
        TotalTokens const &) = default;
#else
    /**
     * Equality comparison operators (C++17 fallback).
     * In C++20+, these are synthesized from operator<=>.
     */
    friend constexpr bool operator == (
        TotalTokens const & lhs,
        TotalTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() ==
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value == rhs.value;
    }

    friend constexpr bool operator != (
        TotalTokens const & lhs,
        TotalTokens const & rhs)
    noexcept(noexcept(std::declval<std::uint32_t const &>() !=
        std::declval<std::uint32_t const &>()))
    {
        return lhs.value != rhs.value;
    }
#endif
#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif
};
} // namespace chat
} // namespace wjh


#if __cplusplus < 202002L
template <>
struct std::hash<wjh::chat::TotalTokens>
: atlas::atlas_detail::hash_base<wjh::chat::TotalTokens>
{};
#endif
#endif // WJH_CHAT_99907B3D5E158A904F8A5A49493807EF0C5F5473
