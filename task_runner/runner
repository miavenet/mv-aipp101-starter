#!/usr/bin/env python3
"""Entry point of the task runner. No install step: run it from a checkout."""

import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.realpath(__file__)), "src"))

from taskrunner.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
